---
doc_type: how-to
ai_scope: none
status: active
created: 2026-01-23
updated: 2026-01-24
tags:
  - documentation
  - meta
  - workflow
---

# Update Workflow

> Как обновлять документацию по результатам аудита или после изменений в коде.

---

## Когда обновлять

| Триггер | Действие |
|---------|----------|
| Аудит выявил расхождения | Обновить по рекомендациям из baseline |
| PR с изменением API/конфигурации | Обновить затронутые документы |
| Новая фича реализована | Добавить в соответствующие документы |
| Рефакторинг модулей | Обновить architecture.md, pipeline/*.md |

---

## Процесс обновления

### Шаг 1: Определить scope

Перед обновлением определи:

1. **Какие документы затронуты?**
   - Из аудита: смотри раздел "Рекомендации"
   - После PR: какие модули/API изменились?

2. **Тип изменений:**
   - Добавление (новый endpoint, модуль, параметр)
   - Исправление (документация не соответствует коду)
   - Удаление (функционал удалён)

3. **Приоритет:**
   - Высокий: API, конфигурация, архитектура
   - Средний: pipeline этапы, how-to
   - Низкий: research, старые proposals

---

### Шаг 2: Выполнить промпт в Claude Code

Для каждого документа выполни соответствующий промпт из раздела [Примеры промптов по папкам](#примеры-промптов-по-папкам).

> [!note] Поведение Claude Code
> В режиме автоматического редактирования Claude Code может:
> - **Спросить подтверждение** — обычно в первом запросе сессии
> - **Сразу применить изменения** — в последующих запросах
> 
> Это нормальное поведение. Если нужен контроль, добавь в промпт: "НЕ применяй изменения пока я не подтвержу"

---

### Шаг 3: Отметить прогресс в baseline

После обновления документов (одного или нескольких) отметь в baseline:

```
Отметь в baseline-YYYY-MM-DD.md что документы обновлены.
Зачеркни выполненные пункты, добавь ✅ и сегодняшнюю дату.
```

> [!tip] Новые issues в процессе
> Если при обновлении обнаружились новые расхождения — Claude Code добавит их в baseline как "требует ревью". Это нормально.

---

### Шаг 4: Коммит

```bash
git add docs/<изменённые файлы>
git commit -m "docs: update <document> to match v0.XX.X"
```

Или попросить Claude Code:
```
Закоммить изменения в документации
```

---

## Практические заметки

### Размер документов

- **1000+ строк для Reference — нормально.** api-reference.md может быть большим если много endpoints
- **Большой документ может занять 50%+ контекста сессии.** Планируй соответственно

### Сколько документов за сессию

| Тип документа | Ожидаемое количество за сессию |
|---------------|-------------------------------|
| api-reference.md | 1 (занимает много контекста) |
| configuration.md, architecture.md | 2-3 |
| pipeline/*.md | 3-5 |
| Мелкие документы (README, CONTRIBUTING) | 4-6 |

> [!warning] Обрабатывай файлы по одному
> Не пытайся обновить все файлы одним промптом. Лучше последовательно: промпт → результат → следующий промпт.

### Порядок работы в сессии

```
1. Промпт для документа 1 → Claude Code обновляет
2. Промпт для документа 2 → Claude Code обновляет
3. ...
4. Отметить прогресс в baseline (общий запрос)
5. Коммит
```

### Валидация

Отдельная валидация обычно не нужна — Claude Code проверяет корректность в процессе обновления.

---

## Примеры промптов по папкам

### Корневые документы — высокий приоритет

#### api-reference.md

```
Обнови docs/api-reference.md

## Расхождения из аудита
- Недокументированные endpoints: /api/step/parse, /api/step/transcribe, 
  /api/step/chunk, /api/step/save
- GET /api/models/default содержит поле `chunk`, которого нет в реализации
- POST /api/step/summarize — отсутствует раздел Request

## Задача
1. Найди все роутеры в backend/app/api/
2. Сравни с документированными endpoints
3. Покажи какие endpoints добавить/исправить
```

#### configuration.md

```
Обнови docs/configuration.md

## Расхождения из аудита
- LONGREAD_MODEL документирована, но не в docker-compose.yml
- LOG_LEVEL_CHUNKER документирована, но не реализована
- ANTHROPIC_API_KEY используется через os.getenv, не в Settings

## Задача
1. Прочитай backend/app/config.py — класс Settings
2. Прочитай docker-compose.yml — секция environment
3. Сравни с документацией
4. Покажи: что добавить, что удалить, что исправить
```

---

### Корневые документы — средний приоритет

#### architecture.md

```
Обнови docs/architecture.md

## Расхождения из аудита
Неупомянутые модули:
- backend/app/services/audio_extractor.py
- backend/app/services/outline_extractor.py
- backend/app/services/progress_estimator.py
- backend/app/api/models_routes.py
- config/prompts/longread/combine.md, section.md
- config/prompts/story/template.md

## Задача
1. Проверь структуру backend/app/services/
2. Проверь структуру config/prompts/
3. Покажи какие модули добавить в документацию
```

#### logging.md

```
Обнови docs/logging.md

## Расхождения из аудита
- LOG_LEVEL_CHUNKER документирован в таблице, но не реализован в config.py
- Упоминается chunker.py, но чанкирование теперь детерминированное (v0.26+)

## Задача
1. Прочитай backend/app/config.py — какие LOG_LEVEL_* реально существуют?
2. Прочитай backend/app/stages/chunk.py — как работает текущий chunker?
3. Покажи что нужно удалить/обновить в документации
```

#### README.md

```
Проверь docs/README.md

## Контекст из аудита
Статус: 🔍 Требует ревью — последнее изменение 2026-01-10 (v0.5)

## Задача
1. Прочитай текущий docs/README.md
2. Сравни структуру проекта с реальной
3. Проверь ссылки на другие документы — все существуют?
4. Покажи что устарело и требует обновления
```

#### CONTRIBUTING.md

```
Проверь docs/CONTRIBUTING.md

## Контекст из аудита
Статус: 🔍 Требует ревью — последнее изменение 2026-01-10

## Задача
1. Прочитай текущий docs/CONTRIBUTING.md
2. Актуальны ли инструкции по setup окружения?
3. Актуальны ли команды для запуска/тестирования?
4. Покажи что требует обновления
```

#### DOCUMENTATION_GUIDELINES.md

```
Проверь docs/DOCUMENTATION_GUIDELINES.md

## Контекст из аудита
Статус: 🔍 Требует ревью — начальная версия 2026-01-09

## Задача
1. Прочитай текущий docs/DOCUMENTATION_GUIDELINES.md
2. Сравни с нашей новой системой в docs/meta/
3. Определи: обновить под новый стандарт или пометить deprecated?
4. Покажи рекомендации
```

---

### Pipeline документация

#### Универсальный промпт для любого этапа

```
Обнови docs/pipeline/<filename>.md

## Задача
1. Прочитай backend/app/stages/<stage_name>.py
2. Сравни с документацией:
   - Input/Output актуальны?
   - Описание логики соответствует коду?
   - Упомянутые промпты существуют в config/prompts/?
3. Покажи что нужно обновить
```

> [!tip] Минимальный вариант
> ```
> Обнови docs/pipeline/<filename>.md
> Сравни с реализацией в backend/app/stages/ и покажи расхождения.
> ```

---

## Обновление по типам документов

### API Reference

**Источники истины:**
- `backend/app/api/*.py` — роутеры
- `backend/app/schemas/*.py` — Pydantic модели

**Что проверять:**
- Все endpoints в таблице
- Request/Response схемы актуальны

---

### Configuration

**Источники истины:**
- `backend/app/config.py` — Settings класс
- `docker-compose.yml` — defaults

**Что проверять:**
- Все ENV переменные документированы
- Defaults совпадают
- Нет "мёртвых" переменных

---

### Architecture

**Источники истины:**
- Структура папок `backend/app/`, `frontend/src/`
- Imports в модулях
- ADR

**Что проверять:**
- Все ключевые модули упомянуты
- Зависимости между компонентами верны

---

### Pipeline этапы

**Источники истины:**
- `backend/app/stages/*.py`
- `config/prompts/`
- `backend/app/schemas/enums.py`

**Что проверять:**
- Input/Output соответствует коду
- Промпты актуальны

---

## После обновления

### Формат commit message

- `docs: update api-reference — add step endpoints`
- `docs: fix configuration — remove dead env vars`
- `docs: sync architecture with v0.59 modules`

---

## Чеклист сессии обновления

```
□ Определить scope (какие документы обновлять)
□ Выполнить промпты последовательно
□ Отметить прогресс в baseline
□ Коммит
```

---

## Антипаттерны

### ❌ Несколько файлов одним промптом

```
Плохо: "Обнови api-reference.md, configuration.md и architecture.md"
Хорошо: Три отдельных промпта, последовательно
```

### ❌ Не отметить прогресс

```
Плохо: Обновил 3 документа, закрыл сессию, забыл где остановился
Хорошо: В конце сессии — запрос на обновление baseline
```

### ❌ Забыть про связанные документы

```
Плохо: Обновил api-reference.md, но pipeline/09-api.md ссылается на старое
Хорошо: Проверил какие документы ссылаются на изменённый раздел
```

---

## Связанные документы

- [Audit Process](audit.md) — как проводить аудит
- [Frontmatter Standard](../standards/frontmatter.md) — обновление поля `updated`
- [Diátaxis+ Framework](../framework.md) — типы документов

---

## Changelog

| Дата | Изменение |
|------|-----------|
| 2026-01-24 | Универсальный промпт для pipeline вместо конкретных файлов |
| 2026-01-24 | Добавлены практические заметки по результатам применения |
| 2026-01-24 | Убран раздел валидации, упрощён процесс |
| 2026-01-24 | Уточнено поведение Claude Code в авто-режиме |
| 2026-01-23 | Создан документ |
