---
related:
  - "[[Среда разработки — MOC]]"
  - "[[Архитектура среды разработки]]"
  - "[[Архитектура локальных AI сервисов]]"
  - "[[Локальные AI сервисы — MOC]]"
  - "[[Домашний сервер — MOC]]"
  - "[[Конфигурация сервера]]"
created: 2025-01-08
tags:
  - homelab
  - development
  - video
  - transcription
  - ai
  - whisper
  - ollama
  - knowledge-base
updated: 2025-01-09
status: mvp-ready
priority: high
---

# БЗ2: Транскрибатор видео — архитектура

## Обзор

Система для автоматической транскрипции и саммаризации видеозаписей обучающих мероприятий. Часть проекта **БЗ2** — базы знаний для команды консультантов по питанию Herbalife.

### Названия

| Назначение | Название |
|------------|----------|
| UI (интерфейс) | **БЗ2: Транскрибатор видео** |
| Техническое | **bz2-video-transcriber** |

### Цели

| Цель | Описание |
|------|----------|
| **Транскрипция** | Преобразование видео в текст с помощью Whisper |
| **Очистка** | Удаление слов-паразитов и нормализация терминологии |
| **Саммаризация** | Создание краткого содержания с классификацией для БЗ 2.0 |
| **Chunking** | Разбиение на смысловые блоки для RAG-поиска |
| **Организация** | Структурированное хранение по дате и типу мероприятия |

### Источник данных

Записи Понедельничной Школы (ПШ) — внутреннее обучение с двумя потоками:

| Поток                | Код | Аудитория               |
| -------------------- | --- | ----------------------- |
| Супервайзеры         | SV  | Консультанты по питанию |
| Независимые партнёры | НП  | Начинающие консультанты |

В дальнейшем предполагаются записи с других типов мероприятий.

---

## Интеграция с локальными AI сервисами

> [!important] Ключевое решение
> БЗ2 использует **общую инфраструктуру AI сервисов** вместо встроенных компонентов.
> Это упрощает приложение и исключает дублирование ресурсов.

### Используемые сервисы

| Сервис | Адрес | Назначение в БЗ2 |
|--------|-------|------------------|
| **Ollama** | http://100.64.0.1:11434 | Очистка, chunking, саммаризация |
| **faster-whisper** | http://100.64.0.1:9000 | Транскрипция видео |

### Модели

| Задача | Модель | Обоснование |
|--------|--------|-------------|
| Транскрипция | large-v3 | Лучшее качество для русского |
| Очистка текста | qwen2.5:14b | Отличный русский, понимание контекста |
| Chunking | qwen2.5:14b | Смысловое разбиение требует понимания |
| Саммаризация | qwen2.5:14b | Качественные саммари на русском |

> [!note] Почему qwen2.5:14b, а не llama3.1:8b?
> - Qwen2.5 обучен на значительно большем корпусе русского текста
> - Для работы с терминологией Herbalife важно точное понимание русского контекста
> - 14B параметров дают лучшее качество при работе с длинными текстами

### Преимущества интеграции

| Аспект | Встроенные компоненты | Общие сервисы ✅ |
|--------|----------------------|------------------|
| Размер образа | ~15 GB (с моделями) | ~500 MB |
| Дублирование моделей | Да | Нет |
| Обновление моделей | Пересборка образа | Независимо |
| Использование GPU | Требует настройки | Уже настроено |
| Код приложения | Сложный | Простой (HTTP вызовы) |

---

## Документация

| Документ                                      | Описание                                  |
| --------------------------------------------- | ----------------------------------------- |
| **[architecture.md](architecture.md)**        | Этот документ — схема системы, компоненты |
| **[pipeline/](pipeline/)**                    | Детальный pipeline обработки (6 этапов)   |
| **[data-formats.md](data-formats.md)**        | Форматы файлов для БЗ 2.0                 |
| **[deployment.md](deployment.md)**            | Развёртывание на TrueNAS                  |
| **[api-reference.md](api-reference.md)**      | HTTP API для Ollama и Whisper             |

---

## Архитектура системы

### Общая схема

```
┌─────────────────────────────────────────────────────────────────┐
│  Клиенты                                                        │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐                             │
│  │   Браузер    │  │  Mac (Ollama)│                             │
│  │   Web UI     │  │   клиент     │                             │
│  └──────┬───────┘  └──────────────┘                             │
│         │                                                       │
└─────────┼───────────────────────────────────────────────────────┘
          │ HTTP / WebSocket
          │ Tailscale (100.64.0.1)
          │
┌─────────┼───────────────────────────────────────────────────────┐
│  TrueNAS│SCALE                                                  │
│         │                                                       │
│         ▼                                                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  bz2-video-transcriber (:8801)                          │    │
│  │                                                         │    │
│  │  ┌─────────┐  ┌────────────┐  ┌────────────┐            │    │
│  │  │ Web UI  │  │  FastAPI   │  │  Pipeline  │            │    │
│  │  │ (React) │◄─┤  Backend   │◄─┤            │            │    │
│  │  └─────────┘  └─────┬──────┘  │ 1.Parser   │            │    │
│  │                     │         │ 2.Whisper ─┼──► API     │    │
│  │                     │         │ 3.Cleaner ─┼──► API     │    │
│  │                     │         │ 4.Chunker ─┼──► API     │    │
│  │                     │         │ 5.Summary ─┼──► API     │    │
│  │                     │         │ 6.Saver    │            │    │
│  │                     │         └──────┬─────┘            │    │
│  └─────────────────────┼────────────────┼──────────────────┘    │
│                        │                │                       │
│            ┌───────────┴────────────────┴───────────┐           │
│            │                                        │           │
│            ▼                                        ▼           │
│  ┌──────────────────────────┐    ┌──────────────────────────┐   │
│  │  ollama (:11434)         │    │  faster-whisper (:9000)  │   │
│  │                          │    │                          │   │
│  │  ┌────────────────────┐  │    │  ┌────────────────────┐  │   │
│  │  │  qwen2.5:14b       │  │    │  │  large-v3          │  │   │
│  │  │  ~9 GB VRAM        │  │    │  │  ~3 GB VRAM        │  │   │
│  │  └────────────────────┘  │    │  └────────────────────┘  │   │
│  │                          │    │                          │   │
│  │  Задачи:                 │    │  Задачи:                 │   │
│  │  • Cleaner               │    │  • Транскрипция          │   │
│  │  • Chunker               │    │                          │   │
│  │  • Summarizer            │    │                          │   │
│  └──────────────────────────┘    └──────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Файловая система                                        │   │
│  │                                                          │   │
│  │  /mnt/main/media/bz2-transcriber/                        │   │
│  │  ├── inbox/      ← Входящие видео (watcher мониторит)    │   │
│  │  ├── archive/    ← Обработанные (структура по датам)     │   │
│  │  └── temp/       ← Временные файлы обработки             │   │
│  │                                                          │   │
│  │  /mnt/apps-pool/dev/projects/bz2-video-transcriber/      │   │
│  │  └── config/     ← Промпты, глоссарий, настройки         │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  GPU: RTX 5070 Ti 16GB (shared: Whisper + Ollama)               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Компоненты

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Web UI** | React + Vite + TanStack Query + Tailwind | Интерфейс управления |
| **Backend** | FastAPI + WebSocket | API, оркестрация |
| **Transcriber** | HTTP → faster-whisper API | Вызов внешнего сервиса |
| **Cleaner** | HTTP → Ollama API | Очистка транскрипта |
| **Chunker** | HTTP → Ollama API | Смысловое разбиение |
| **Summarizer** | HTTP → Ollama API | Саммаризация + классификация |

---

## Pipeline обработки

> **Детальное описание:** [pipeline/](pipeline/)

### Краткая схема

```
┌─────────────────────────────────────────────────────────────────┐
│                      VIDEO PROCESSING PIPELINE                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐       │
│  │ 1.PARSE │───▶│2.WHISPER│───▶│3.CLEAN  │───▶│4.CHUNK  │       │
│  │ filename│    │ → API   │    │ → API   │    │ → API   │       │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘       │
│                                                   │             │
│                                                   ▼             │
│                              ┌─────────┐    ┌─────────┐         │
│                              │ 6.SAVE  │◀───│5.SUMMARY│         │
│                              │  files  │    │ → API   │         │
│                              └─────────┘    └─────────┘         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Этапы

| # | Этап | Инструмент | Описание |
|---|------|------------|----------|
| 1 | Parse | Python regex | Извлечение метаданных из имени файла |
| 2 | Transcribe | faster-whisper API | GPU транскрипция видео |
| 3 | Clean | Ollama API + Glossary | Удаление паразитов, нормализация терминов |
| 4 | Chunk | Ollama API | Разбиение на смысловые блоки (100-400 слов) |
| 5 | Summarize | Ollama API | Саммари + классификация для БЗ 2.0 |
| 6 | Save | Python | Сохранение в структурированный архив |

### Примеры API вызовов

**Транскрипция (faster-whisper):**

```python
import requests

def transcribe(video_path: str) -> str:
    """Транскрибировать видео через внешний API."""
    with open(video_path, "rb") as f:
        response = requests.post(
            "http://100.64.0.1:9000/v1/audio/transcriptions",
            files={"file": f},
            data={
                "language": "ru",
                "response_format": "text"
            }
        )
    return response.text
```

**Очистка текста (Ollama):**

```python
def clean_transcript(text: str, glossary: dict) -> str:
    """Очистить транскрипт с использованием глоссария."""
    prompt = f"""Ты — редактор текста. Задачи:
1. Удали слова-паразиты (эм, ну, как бы, вот, типа)
2. Исправь терминологию по глоссарию
3. Разбей на абзацы по смыслу

Глоссарий:
{format_glossary(glossary)}

Текст:
{text}

Очищенный текст:"""
    
    response = requests.post(
        "http://100.64.0.1:11434/api/generate",
        json={
            "model": "qwen2.5:14b",
            "prompt": prompt,
            "stream": False
        }
    )
    return response.json()["response"]
```

---

## Формат файлов

> **Детальное описание:** [docs/data-formats.md](docs/data-formats.md)

### Входящий файл

```
{дата} {тип}.{поток} {тема} ({спикер}).mp4

Пример:
2025.04.07 ПШ.SV Подготовка и проведение Группы поддержки (Светлана Дмитрук).mp4
```

### Выходные файлы

```
/archive/{год}/{месяц}/{тип}.{поток}/{тема} ({спикер})/
├── {оригинальное название}.mp4      # Видео
├── transcript_chunks.json           # Chunks для RAG-индексации
├── summary.md                       # Саммари с классификацией для БЗ 2.0
└── transcript_raw.txt               # Backup оригинала с тайм-кодами
```

### Интеграция с БЗ 2.0

**Первый этап — ручная загрузка:**

1. Транскрибатор создаёт файлы в `/archive/`
2. Оператор копирует в Google Drive `/БЗ2/Видео/`
3. BZ2_Indexer автоматически индексирует

---

## Docker конфигурация

### Размещение на TrueNAS

| Путь | Назначение |
|------|------------|
| `/mnt/apps-pool/dev/projects/bz2-video-transcriber/` | Код приложения |
| `/mnt/main/media/bz2-transcriber/inbox/` | Входящие видео |
| `/mnt/main/media/bz2-transcriber/archive/` | Обработанный архив |
| `/mnt/main/media/bz2-transcriber/temp/` | Временные файлы |

### docker-compose.yml

```yaml
services:
  bz2-transcriber:
    build: ./backend
    container_name: bz2-transcriber
    restart: unless-stopped
    ports:
      - "8801:80"
    volumes:
      - /mnt/main/media/bz2-transcriber:/data:rw
      - ./config:/app/config:ro
    environment:
      - OLLAMA_URL=http://192.168.1.152:11434
      - WHISPER_URL=http://192.168.1.152:9000
      - LLM_MODEL=qwen2.5:14b
      - DATA_ROOT=/data
      - INBOX_DIR=/data/inbox
      - ARCHIVE_DIR=/data/archive
      - WHISPER_LANGUAGE=ru
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  bz2-frontend:
    build: ./frontend
    container_name: bz2-frontend
    restart: unless-stopped
    ports:
      - "8802:80"
    depends_on:
      - bz2-transcriber
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 3
```

> [!note] Нет GPU в docker-compose
> Приложению БЗ2 не нужен прямой доступ к GPU — вся обработка через API внешних сервисов.

### Ресурсы

| Компонент | RAM | CPU | Примечание |
|-----------|-----|-----|------------|
| FastAPI backend | ~100 MB | Minimal | Event loop |
| React frontend | — | — | Статика через nginx |
| File watcher | ~50 MB | Minimal | Мониторинг папки |
| **Итого** | ~200 MB | Minimal | Очень легковесное |

### Зависимости от AI сервисов

| Сервис | Должен быть запущен | Проверка |
|--------|---------------------|----------|
| Ollama | ✅ Да | `curl http://100.64.0.1:11434/api/version` |
| faster-whisper | ✅ Да | `curl http://100.64.0.1:9000/health` |

---

## Web UI

### Технологии

| Технология | Назначение |
|------------|------------|
| React 18 + TypeScript | Основа UI |
| Vite | Сборка и dev-сервер |
| TanStack Query | Кэширование API запросов |
| Tailwind CSS v4 | Стилизация |
| Axios | HTTP клиент |
| lucide-react | Иконки |

### Dashboard

```
┌─────────────────────────────────────────────────────────────────┐
│  БЗ2 Транскрибатор            [● Whisper] [● Ollama]            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────┐  ┌─────────────────────────┐   │
│  │  Inbox (3 видео)        [↻] │  │  Задачи (2)             │   │
│  │  ┌─────────────────────────┐│  │  ┌─────────────────────┐│   │
│  │  │ 📹 Группа поддержки  [▶]││  │  │ [████████░░] 65%    ││   │
│  │  │ 📹 Работа с возр.    [▶]││  │  │ Очистка текста...   ││   │
│  │  │ 📹 Первые шаги       [▶]││  │  ├─────────────────────┤│   │
│  │  └─────────────────────────┘│  │  │ ✓ Завершено (вчера) ││   │
│  └─────────────────────────────┘  │  └─────────────────────┘│   │
│                                   └─────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### Режимы обработки

При клике на "Обработать" открывается модальное окно с выбором:

| Режим | Описание |
|-------|----------|
| **Полный pipeline** | Автоматически все 6 этапов, прогресс через WebSocket |
| **Пошаговый** | Выполнение этапов по одному с просмотром результатов |

### Пошаговый режим

```
┌─────────────────────────────────────────────────────────────────┐
│  Пошаговая обработка: video.mp4                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Шаги: [✓]──[✓]──[●]──[○]──[○]──[○]                            │
│         1    2    3    4    5    6                              │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Шаг 3: Очистка текста                   [Выполнить →]  │    │
│  │  Очистка текста с использованием глоссария и LLM        │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
│  Результаты:                                                    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Метаданные                                              │    │
│  │  Дата: 2025-01-09 | Поток: ПШ.SV | Спикер: Иванова      │    │
│  ├─────────────────────────────────────────────────────────┤    │
│  │  Сырая транскрипция (45 мин, 1247 сегментов)            │    │
│  │  [00:00:00] Добрый день, коллеги...                     │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Конфигурация

### Структура config/

```
config/
├── prompts/
│   ├── cleaner.md           # Промпт очистки транскрипта
│   ├── chunker.md           # Промпт смыслового разбиения
│   └── summarizer.md        # Промпт саммаризации
├── glossary.yaml            # Глоссарий терминов Herbalife
└── events.yaml              # Типы мероприятий
```

### config/events.yaml

```yaml
# Типы мероприятий
event_types:
  ПШ:
    name: "Понедельничная Школа"
    streams:
      SV: "Супервайзеры"
      НП: "Независимые партнёры"

# Формат даты в имени файла
date_format: "%Y.%m.%d"

# Паттерн имени файла
filename_pattern: "{date} {type}.{stream} {title} ({speaker})"
```

### config/glossary.yaml

> **Полный файл:** [docs/data-formats.md#glossary](docs/data-formats.md#glossary)

```yaml
# Терминология Herbalife для нормализации
products:
  - canonical: "Формула 1"
    variations: ["формула один", "формула 1", "Ф1", "ф-1"]
  
brand:
  - canonical: "Herbalife"
    variations: ["гербалайф", "хербалайф", "герба лайф"]

business:
  - canonical: "Группа поддержки"
    variations: ["группа поддержки", "гп"]
```

---

## Структура проекта

```
bz2-video-transcriber/
├── CLAUDE.md                    # Entry point для Claude Code
├── docs/
│   ├── architecture.md          # Этот документ
│   ├── pipeline/                # Детальный pipeline (6 этапов)
│   ├── data-formats.md          # Форматы файлов
│   └── deployment.md            # Развёртывание
│
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI app
│   │   ├── config.py            # Настройки
│   │   ├── routers/
│   │   │   ├── files.py         # API для файлов
│   │   │   ├── jobs.py          # API для задач
│   │   │   ├── settings.py      # API настроек
│   │   │   └── websocket.py     # WebSocket прогресс
│   │   ├── services/
│   │   │   ├── parser.py        # Парсинг имени файла
│   │   │   ├── watcher.py       # Мониторинг inbox
│   │   │   ├── transcriber.py   # HTTP → Whisper API
│   │   │   ├── cleaner.py       # HTTP → Ollama API
│   │   │   ├── chunker.py       # HTTP → Ollama API
│   │   │   ├── summarizer.py    # HTTP → Ollama API
│   │   │   └── pipeline.py      # Оркестрация
│   │   └── models/
│   │       └── schemas.py       # Pydantic модели
│   └── requirements.txt
│
├── frontend/
│   ├── Dockerfile               # Docker образ (node build → nginx)
│   ├── nginx.conf               # Proxy для API и WebSocket
│   ├── package.json
│   ├── vite.config.ts           # Vite + proxy + path alias
│   ├── tailwind.config.js
│   └── src/
│       ├── App.tsx              # Главный компонент
│       ├── main.tsx
│       ├── index.css            # Tailwind imports
│       ├── api/
│       │   ├── types.ts         # TypeScript типы (из backend schemas)
│       │   ├── client.ts        # Axios instance
│       │   ├── websocket.ts     # WebSocket hook
│       │   └── hooks/           # TanStack Query hooks
│       │       ├── useInbox.ts
│       │       ├── useJobs.ts
│       │       ├── useServices.ts
│       │       ├── useProcess.ts
│       │       └── useSteps.ts
│       └── components/
│           ├── layout/          # Header, Layout
│           ├── common/          # Button, Card, Badge, ProgressBar, Spinner, Modal
│           ├── services/        # ServiceStatus (Whisper/Ollama индикаторы)
│           ├── inbox/           # InboxList, VideoItem
│           ├── jobs/            # JobList, JobCard
│           ├── processing/      # ProcessingModal, FullPipeline, StepByStep
│           └── results/         # MetadataView, TranscriptView, ChunksView, SummaryView
│
├── config/
│   ├── prompts/
│   │   ├── cleaner.md
│   │   ├── chunker.md
│   │   └── summarizer.md
│   ├── glossary.yaml
│   └── events.yaml
│
├── docker-compose.yml
├── Dockerfile
├── nginx.conf
└── scripts/
    └── deploy.sh
```

---

## API Endpoints

### Inbox & Jobs

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/api/inbox` | Список видео в inbox |
| GET | `/api/jobs` | Список всех задач |
| GET | `/api/jobs/{id}` | Статус задачи |
| POST | `/api/process` | Запуск полного pipeline |

### Step-by-step (пошаговый режим)

| Method | Endpoint | Описание |
|--------|----------|----------|
| POST | `/api/step/parse` | Парсинг метаданных |
| POST | `/api/step/transcribe` | Транскрипция (Whisper) |
| POST | `/api/step/clean` | Очистка текста |
| POST | `/api/step/chunk` | Разбиение на чанки |
| POST | `/api/step/summarize` | Саммаризация |
| POST | `/api/step/save` | Сохранение в архив |

### Health

| Method | Endpoint | Описание |
|--------|----------|----------|
| GET | `/health` | Статус приложения |
| GET | `/health/services` | Статус AI сервисов (Whisper, Ollama) |

### WebSocket

| Endpoint | Описание |
|----------|----------|
| `/ws/{job_id}` | Прогресс задачи в реальном времени |

---

## Доступ

| Сервис | Локально | Tailscale |
|--------|----------|-----------|
| Frontend (Web UI) | http://192.168.1.152:8802 | http://100.64.0.1:8802 |
| Backend (API) | http://192.168.1.152:8801 | http://100.64.0.1:8801 |

### Зависимости (должны быть доступны)

| Сервис | Адрес | Документация |
|--------|-------|--------------|
| Ollama API | http://100.64.0.1:11434 | [[Установка Ollama на TrueNAS]] |
| faster-whisper API | http://100.64.0.1:9000 | [[Установка Whisper на TrueNAS]] |

---

## План реализации

| Фаза | Задача | Статус |
|------|--------|--------|
| 1-3 | Структура проекта, docs, filename parser | ✅ Выполнено |
| 4-5 | Сервисы pipeline (Whisper, Cleaner, Chunker, Summarizer) | ✅ Выполнено |
| 6 | Saver — сохранение в архив | ✅ Выполнено |
| 7-9 | AI Client, Prompt Manager, Glossary | ✅ Выполнено |
| 10 | Pipeline Orchestrator | ✅ Выполнено |
| 11 | FastAPI endpoints | ✅ Выполнено |
| 12 | Docker deployment | ✅ Выполнено |
| 13 | Frontend (React + Vite) | ✅ Выполнено |

> [!success] MVP готов
> Все основные фазы реализованы. Система готова к использованию.

---

## Связанные документы

### Локальные AI сервисы
- [[Локальные AI сервисы — MOC]] — навигация
- [[Архитектура локальных AI сервисов]] — общая архитектура
- [[Установка Ollama на TrueNAS]] — сервер LLM
- [[Установка Whisper на TrueNAS]] — сервер транскрипции

### Среда разработки
- [[Среда разработки — MOC]] — навигация по dev-среде
- [[Архитектура среды разработки]] — общая концепция
- [[Установка Dockge]] — деплой через UI

### Инфраструктура
- [[Конфигурация сервера]] — спецификация железа
- [[Справочник сервисов]] — порты и адреса

---

## Ресурсы

- [faster-whisper](https://github.com/guillaumekln/faster-whisper)
- [Ollama API](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [FastAPI](https://fastapi.tiangolo.com/)
- [Qwen2.5](https://huggingface.co/Qwen/Qwen2.5-14B-Instruct)
