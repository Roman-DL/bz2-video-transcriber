# Справочник команд

> Кастомные команды Claude Code для автоматизации workflow разработки

---

## Обзор команд

| Поведение | Автоматизировано | Ручная команда |
|-----------|-----------------|----------------|
| Preflight | Rules загружаются по globs; CLAUDE.md директива проверять архитектуру | `/preflight` — полный анализ совместимости |
| Finalize | CLAUDE.md секция "После реализации" | `/finalize` — глубокая проверка + drift detection |
| Sync docs | — | `/sync-docs` — полный аудит (всегда ручной) |
| Refactor | — | `/refactor-claude` — при росте CLAUDE.md |

**Важно:** Preflight и finalize имеют встроенное автоматическое поведение через rules и CLAUDE.md, но команды остаются доступны для глубокого анализа сложных случаев. Sync docs и refactor всегда запускаются вручную.

### Различие команд по скоупу

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│  /preflight          /finalize              /sync-docs               │
│  ─────────           ─────────              ──────────               │
│  ДО реализации       ПОСЛЕ реализации       ПЕРИОДИЧЕСКИ             │
│                                                                      │
│  Скоуп: задача       Скоуп: текущие         Скоуп: весь проект       │
│                      изменения                                       │
│                                                                      │
│  "Как вписать        "Что изменилось?       "Всё ли в порядке        │
│   фичу в систему?"    Нужно ли обновить      во всём проекте?"       │
│                       документацию?"                                 │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### /finalize vs /sync-docs

Ключевое различие — **скоуп проверки**:

| | `/finalize` | `/sync-docs` |
|-|-------------|-------------|
| **Что проверяет** | Только затронутые модули | Весь проект целиком |
| **Когда** | После фичи/фазы | Раз в неделю/фазу; при накоплении долга |
| **Drift detection** | Для изменённых файлов | Для всех rules |
| **Карта покрытия** | — | Код → architecture/ → rules/ → requirements |
| **Housekeeping** | Архивация планов, обновление статуса | Диагностика archive-статусов |
| **Покрытие rules** | — | Все ли модули имеют rules? |
| **Устранение долга** | Точечное (текущие изменения) | Системное (все пропуски) |
| **Субагенты** | Нет | Да, для больших проектов |

Простая формула: **finalize = что изменилось сейчас**, **sync-docs = найти и устранить весь накопившийся долг**.

---

## /preflight [задача | план | пусто]

### Назначение

Помочь спроектировать фичу с учётом существующей архитектуры.

> **Главная цель:** понять как построить фичу, чтобы она органично вписалась в систему.

### Автоматическая альтернатива

Rules автоматически загружают контекст модуля и архитектурные ограничения. CLAUDE.md директива автоматически проверяет совместимость при планировании. `/preflight` нужна только для сложных фич, требующих полного анализа совместимости со всеми компонентами системы.

### Когда вызывать

- **Сложные фичи** — перед реализацией, когда нужна полная совместимость со всеми компонентами
- **После планирования в Plan Mode** — проверить готовый план на соответствие архитектуре
- **Перед сложным рефакторингом** — где нужно понять влияние на весь проект
- Когда не уверен в совместимости с архитектурой

### Можно пропустить

- Простой баг (1-2 файла) — rules автоматически загружают контекст модуля
- Продолжение работы в той же сессии
- Рутинные задачи — rules автоматически загружают контекст модуля
- Очевидная задача без архитектурных последствий

### Варианты вызова

```bash
# По файлу плана (новая беседа после планирования)
/preflight docs/plans/auth-oauth.md

# По описанию задачи
/preflight Добавить авторизацию через OAuth

# По контексту беседы (после Plan Mode в той же сессии)
/preflight
```

### Для сложных задач

Если задача требует исследования — сначала Plan Mode:

1. **Plan Mode** — AI исследует код, предлагает подходы, создаёт план
2. **Сохранение плана** — если контекст заканчивается, сохрани план в `docs/plans/`
3. **`/preflight`** — проверка совместимости:
   - В той же сессии: `/preflight` (контекст уже есть)
   - В новой сессии: `/preflight docs/plans/xxx.md`
4. **Корректировка плана** — при необходимости обновить план
5. **Реализация**

### Что делает

1. Изучает ARCHITECTURE.md и `docs/architecture/` — компоненты, связи, паттерны
2. Проверяет ADR — учитывает принятые решения
3. Проверяет CLAUDE.md — ограничения и статус
4. **Рекомендует как интегрировать фичу** — где код, какие паттерны
5. Выявляет потенциальные конфликты
6. Даёт предварительный список документов для обновления (включая `architecture/`)

> Окончательную проверку документации выполнит `/finalize`

### Связанные документы

- [WORKFLOW.md](WORKFLOW.md) — процесс разработки
- [.claude/commands/preflight.md](../.claude/commands/preflight.md) — исходный код команды

---

## /finalize

### Назначение

Страховочная сетка — проверяет фактические изменения и актуализирует документацию.

> Ловит всё, что не было предусмотрено при планировании.

### Автоматическая альтернатива

CLAUDE.md секция "После реализации" автоматически выполняет базовые проверки при завершении работы — проверяет документацию, ограничения и уведомляет об изменениях. `/finalize` нужна для глубокого аудита с drift detection, когда нужно понять насколько фактическая реализация отличается от плана и архитектуры.

### Когда вызывать

- **После сложных фич/фаз** — для глубокого аудита с drift detection
- **После фазовых завершений** — даже если была автоматическая проверка в CLAUDE.md
- **Когда нужна глубокая проверка** — после изменений без планирования, экспериментов
- **При подозрении на drift** — когда документация и код могут разойтись

### Можно пропустить

- Минорные изменения (стилистика, мелкие баги) — секция "После реализации" в CLAUDE.md покрывает базовые проверки
- Рутинные задачи — секция 'После реализации' в CLAUDE.md покрывает базовые проверки
- Точно знаешь что документация актуальна

### Пример

```bash
/finalize
```

### Что делает

1. Анализирует изменения в коде
2. Проверяет ARCHITECTURE.md — нужно ли обновить обзор
3. Проверяет `docs/architecture/` — нужен ли новый документ или обновление существующего
4. Проверяет необходимость нового ADR
5. Проверяет CLAUDE.md — новые ограничения
6. Выполняет housekeeping (archive, cleanup)
7. Проверяет размер CLAUDE.md (предупреждает если > 250 строк)

### Важно: порог контекста

| Контекст беседы | Рекомендация |
|-----------------|--------------|
| < 80% | `/finalize` в текущей беседе, один коммит |
| > 80% | Коммит кода → `/finalize` в новой беседе |

### Связанные документы

- [WORKFLOW.md](WORKFLOW.md) — процесс разработки
- [.claude/commands/finalize.md](../.claude/commands/finalize.md) — исходный код команды

---

## /sync-docs

### Назначение

Аудит документации и устранение накопившегося документационного долга.

> **Главная задача:** найти реализованные фичи без документации и устранить разрыв.
> **Для больших проектов:** автоматически использует субагентов чтобы не переполнить контекст.

### Когда вызывать

- После завершения фазы (обязательно)
- Раз в неделю при активной разработке
- При внедрении методологии в существующий проект
- Если несколько фич реализованы без `/finalize`
- Перед онбордингом нового участника

### Пример

```bash
/sync-docs
```

### Что делает

1. **Строит карту покрытия** — матрица: код → architecture/ → rules/ → requirements
2. **Определяет приоритеты долга:**
   - 🔴 Критический — реализованный модуль без architecture/ документа
   - 🟠 Значительный — модуль без rules, сильно устаревший документ
   - 🟡 Умеренный — устаревший обзор, отсутствующий ADR
3. **Проверяет каждую область** — ARCHITECTURE.md, architecture/, rules/, ADR, CLAUDE.md
4. **Создаёт приоритизированный отчёт**
5. **Устраняет долг** начиная с критических проблем (с подтверждением)

### Стратегия по размеру

| Размер проекта | Подход |
|----------------|--------|
| < 20 файлов | Прямой анализ |
| 20-100 файлов | Субагенты рекомендуются |
| > 100 файлов | Субагенты обязательно |

### Связанные документы

- [.claude/commands/sync-docs.md](../.claude/commands/sync-docs.md) — исходный код команды

---

## /refactor-claude

### Назначение

Рефакторинг CLAUDE.md: вынос правил в модульную структуру `.claude/rules/`.

### Когда вызывать

- CLAUDE.md > 250 строк (предупреждение в `/finalize`)
- CLAUDE.md > 300 строк (критично)
- Сложно найти нужную инструкцию
- Много специфичных правил для разных частей кода

### Пример

```bash
/refactor-claude
```

### Что делает

1. Анализирует размер и структуру CLAUDE.md
2. Категоризирует контент (что оставить, что вынести)
3. Создаёт модульные правила в `.claude/rules/`
4. Добавляет path-фильтры где уместно
5. Сокращает CLAUDE.md до < 300 строк

### Результат

```
project/
├── CLAUDE.md                  # < 300 строк (критичное)
└── .claude/
    └── rules/
        ├── bot-handlers.md    # Правила для обработчиков ботов
        ├── services.md        # Структура сервисов
        ├── database.md        # Работа с БД и ORM
        └── api-endpoints.md   # REST API конвенции
```

### Что остаётся в CLAUDE.md

- Описание проекта
- Команды запуска/тестов/деплоя
- Навигация по документации
- 2-3 самых критичных ограничения
- Workflow разработки

### Связанные документы

- [.claude/commands/refactor-claude.md](../.claude/commands/refactor-claude.md) — исходный код команды

---

## Автоматизация и ручные команды

**80% рутинных проверок автоматизированы:**

- **Rules** загружают контекст и архитектурные ограничения при работе с модулем
- **CLAUDE.md директива** автоматически проверяет план и совместимость при планировании
- **CLAUDE.md секция "После реализации"** автоматически выполняет базовые проверки при завершении

**Разработчику не нужно помнить о вызове команд для рутинной работы.** Rules и CLAUDE.md покрывают стандартные сценарии.

**Команды остаются для глубокого анализа:**

- `/preflight` — для сложных фич, требующих полного анализа совместимости со всеми компонентами
- `/finalize` — для глубокого аудита с drift detection после сложных фаз
- `/sync-docs` — полный аудит документации и правил проекта
- `/refactor-claude` — модуляризация CLAUDE.md по мере роста

---

## Связанные документы

| Документ | Описание |
|----------|----------|
| [WORKFLOW.md](WORKFLOW.md) | Процесс разработки с Claude Code |
| [CONTRIBUTING.md](CONTRIBUTING.md) | Чеклисты по типам изменений |
| [CLAUDE.md](../CLAUDE.md) | Инструкции для Claude Code |

---

_Справочник кастомных команд Claude Code_
