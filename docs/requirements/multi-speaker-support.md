# Поддержка мультиспикерного контента

> Адаптация промптов, чанков и контекстуализации для MD-транскриптов с несколькими спикерами

**Статус:** Draft (инфраструктура реализована в v0.64, остальное — не начато)
**Дата:** 2026-02-19 (обновлено 2026-02-22)
**Зависит от:** [Импорт готовых MD-транскриптов](archive/md-transcript-import.md) (v0.64, реализовано)

---

## 1. Проблема

Текущие промпты longread и summary предполагают одного спикера:
- Longread: "писать от первого лица, сохранять голос автора" + "реплики ведущих → убрать"
- Summary: "цитаты спикера" (единственное число), без атрибуции

> **Примечание (v0.72+):** Clean — только глоссарная подстановка, не удаляет реплики. Проблема "реплики со-спикера удаляются" относится к Longread, не к Clean.

На мероприятиях регулярно встречаются темы с несколькими спикерами. Pipeline обработает их, но качество результата будет ниже — Claude не получает инструкций, как работать с мультиспикерным контентом.

**Только MD-транскрипты:**
Мультиспикерность реализуема только для готовых MD-транскриптов из MacWhisper, где пользователь подставляет имена спикеров. Whisper API не поддерживает diarization — при транскрибации аудио/видео метки спикеров отсутствуют.

Если на регулярном мероприятии тема с несколькими спикерами — пользователь вручную транскрибирует в MacWhisper, подставляет имена и обрабатывает как MD-транскрипт.

---

## 2. Сценарии

### Сценарий A: Со-спикеры (2 именованных спикера)

Два ведущих ведут одну тему, поочерёдно дополняя друг друга. Каждый может рассказывать свой слайд.

```
Беркин Андрей
Добрый день! Сегодня мы поговорим о том, как строить первое поколение.
Я расскажу про стратегию, а Светлана — про конкретные инструменты.

Дмитрук Светлана
Спасибо, Андрей! Начну с инструмента «солнышко». Это визуальная схема...
```

**Особенности:**
- Единая тема, общая логика изложения
- Спикеры дополняют друг друга
- Лонгрид должен быть цельным, но с атрибуцией идей

### Сценарий B: Линейка (3+ именованных спикеров)

Основной спикер начинает тему, затем приглашает участников на сцену. Каждый рассказывает свою мини-тему. Основной спикер может задавать уточняющие вопросы.

```
Дмитрук Светлана
Сегодня у нас тема «Спонсор, за которым идут». Я пригласила трёх
консультантов из моей команды, которые расскажут свои истории.

Беркин Андрей
Я пришёл в бизнес 3 года назад. Светлана была моим спонсором...

Дмитрук Светлана
Андрей, расскажи, что было самым сложным в первый год?

Беркин Андрей
Самое сложное — это первые 10 встреч. Я боялся звонить...

Иванова Мария
А мой путь начался совсем иначе...
```

**Особенности:**
- Основной спикер задаёт рамку и модерирует
- Приглашённые участники — мини-темы внутри общей
- Уточняющие вопросы ведущего — контекст, не мусор
- Лонгрид должен структурировать по участникам

### Сценарий C: Тема + Q&A (именованные спикеры + SpeakerN)

Основная тема одного-двух спикеров, в конце — ответы на вопросы из зала. Задающие вопросы остаются анонимными (`SpeakerN` — дефолт MacWhisper, имена не подставляются).

```
Дмитрук Светлана
...и это всё о работе с возражениями. Есть вопросы?

Speaker3
А что делать, если клиент говорит «мне не нужно»?

Дмитрук Светлана
Отличный вопрос! В этом случае я обычно...

Speaker5
А если клиент уже пользовался продуктом и разочаровался?

Дмитрук Светлана
Это частый случай. Здесь важно...
```

**Особенности:**
- Основная часть — стандартная тема
- `SpeakerN` — анонимные участники из зала (дефолт MacWhisper)
- Лонгрид выделяет Q&A в отдельный раздел

---

## 3. Решение

### Программное определение сценария

Тип мультиспикерского контента определяется по анализу MD-файла:

1. **Извлечь уникальных спикеров** — строки-метки (`Фамилия Имя` или `SpeakerN`)
2. **Разделить на именованных и анонимных** — `SpeakerN` = анонимный
3. **Определить сценарий:**

| Именованных спикеров | Есть SpeakerN | Сценарий |
|----------------------|---------------|----------|
| 1 | Нет | Односпикерный (текущий pipeline) |
| 1 | Да | **C: Тема + Q&A** |
| 2 | Нет | **A: Со-спикеры** |
| 2 | Да | **A: Со-спикеры + Q&A** |
| 3+ | Нет | **B: Линейка** |
| 3+ | Да | **B: Линейка + Q&A** |

### Передача в промпт

Информация о мультиспикерности передаётся в промпт как часть метаданных:

```
Тип: мультиспикер / со-спикеры
Спикеры: Дмитрук Светлана, Беркин Андрей
Анонимные участники: Speaker3, Speaker5 (вопросы из зала)
```

Claude получает конкретные инструкции для определённого сценария, а не пытается угадать формат.

### Ключевые идеи

1. **Программное определение** — сценарий определяется кодом из содержимого MD-файла, не Claude
2. **Контекст в промпте** — список спикеров и тип сценария передаются явно
3. **Условные инструкции** — промпты содержат секции для каждого сценария
4. **Изменения в промптах + минимальный код** — парсинг спикеров и формирование контекста

---

## 4. Изменения в промптах

### Стратегия: единый файл с условными секциями

Мультиспикерные инструкции добавляются **в существующие промпт-файлы** как дополнительные секции, а не как отдельные файлы. Обоснование:

- **80% инструкций общие** для всех сценариев (структура, стиль, глоссарий, markdown-правила). Отдельные файлы → дублирование и рассинхрон
- **Контекст сценария — в user prompt** (template variables): тип, список спикеров, роли. Инструкции остаются стабильными, контекст — динамический
- **Размер промпта не проблема** — ~200-300 слов условных секций, Claude легко обрабатывает

**Разделение ответственности:**
- `instructions.md` — правила ("если мультиспикер, то...")
- User prompt (template) — контекст ("Тип: линейка, Спикеры: ..., Ведущий: ...")

### ~~4.1 Clean~~ — не требуется (v0.72+)

> С v0.72 Clean — только глоссарная подстановка терминов. Метки спикеров уже сохраняются (правило 4 в `cleaning/system.md`). Clean не удаляет реплики и не принимает решений о контенте — мультиспикерная секция здесь не нужна.
>
> Проблема "реплики со-спикера удаляются" решается в Longread (§4.2), который содержит инструкцию "Реплики ведущих → убрать".

### 4.2 Longread (`config/prompts/longread/instructions.md`)

**Текущие проблемы:**
- "Голос автора — от первого лица" — не работает для нескольких спикеров
- "Реплики ведущих → убрать" (секция "Что убирать") — может удалить реплики со-спикера или уточняющие вопросы ведущего линейки

**Добавить секцию "Мультиспикерный контент" в конце `instructions.md`:**

| Сценарий | Стратегия лонгрида |
|----------|-------------------|
| **Со-спикеры** | Единый текст от третьего лица, атрибуция через "по словам {Имя}" или "как отметил(а) {Имя}". Логика изложения — общая |
| **Линейка** | Вступление ведущего → отдельный раздел (H2) для каждого участника. Уточняющие вопросы ведущего вплетаются в текст участника |
| **+ Q&A** | Дополнительный раздел "Вопросы и ответы" в конце. Формат: **Вопрос:** / **Ответ:** без указания кто спрашивал |

Для линейки — каждый участник получает свой H2-раздел:
```markdown
## Введение (Дмитрук Светлана)

Сегодня мы поговорим о спонсорстве...

## Путь в бизнес (Беркин Андрей)

Я пришёл в бизнес 3 года назад...

## Первые шаги (Иванова Мария)

Мой опыт начался с того, что...

## Вопросы и ответы

**Вопрос:** Что делать, если клиент говорит «мне не нужно»?
**Ответ:** В этом случае...
```

### 4.3 Summary (`config/prompts/summary/instructions.md`)

**Текущая проблема:**
- Цитаты без атрибуции

**Добавить секцию "Мультиспикерный контент" в конце `instructions.md`:**
- Цитаты с атрибуцией: `> «Цитата» — Беркин А.`
- Для линейки — мини-конспект каждого участника как подраздел общего конспекта
- Для Q&A — блок "Вопросы и ответы" в формате **Вопрос:** / **Ответ:**

**Линейка в summary — структура:**
```markdown
## Ключевые мысли

### Беркин Андрей
- Пришёл в бизнес 3 года назад
- Самое сложное — первые 10 встреч
> «Я боялся звонить, но спонсор научил меня...» — Беркин А.

### Иванова Мария
- Начала с онлайн-формата
- Ключ к росту — регулярность
> «Каждый день — одна встреча, без исключений» — Иванова М.
```

### 4.4 Story (`config/prompts/story/instructions.md`)

**Минимальные изменения (секция в конце `instructions.md`):**
- Story уже поддерживает пары (`"names": "Имя1 и Имя2"`)
- Линейка на выездных — это скорее educational (longread), не leadership (story)
- Уточнить: если в leadership-теме два со-спикера — атрибутировать идеи каждому

---

## 5. Изменения в коде

### Реализовано (v0.64)

- `SpeakerInfo` модель в `schemas.py` — `named_speakers`, `anonymous_speakers`, `scenario`
- `speaker_utils.py` — `parse_speakers()`, `_determine_scenario()` + 6 self-tests
- `VideoMetadata.speaker_info` — заполняется в `step_routes.py` для MD-транскриптов
- Clean prompt — правило сохранения меток спикеров (строка 4 в `system.md`)

> Сейчас `speaker_info` используется только для UI. Stages и saver его не читают.

### Остаётся: data flow speaker_info → промпты → saver

```
1. Longread/Summary/Story stages:
   speaker_info = context.get_result("parse").speaker_info
   → формирует блок контекста для user prompt:
     "Тип: линейка\nСпикеры: ...\nВедущий: ..."

2. Saver (_save_chunks_json):
   speaker_info = metadata.speaker_info
   → адаптирует шапку чанка по сценарию
```

### Контекст в user prompt

Информация о мультиспикерности передаётся в user prompt каждого LLM-этапа как template variable:

```
## Мультиспикерный контент
Тип: линейка
Спикеры: Дмитрук Светлана (ведущий), Беркин Андрей, Иванова Мария
Есть Q&A: да (Speaker3, Speaker5)
```

Для односпикерных транскриптов (`speaker_info is None` или `scenario == "single"`) — блок не добавляется, промпты работают как раньше.

---

## 6. Чанки и контекстуализация для RAG

Мультиспикерные темы загружаются в RAG базу знаний (BZ2-Bot). Шапка чанка и структура должны адаптироваться под сценарий.

### Текущий формат шапки (односпикерный)

```
Тема: Закрытие полной оценки
Спикер: Иванова А. | ПШ — Супервайзеры | 22.01.2026

## Подготовка к встрече

{текст}
```

### Со-спикеры — адаптация шапки

Два спикера в шапке, единый текст:

```
Тема: Как строить первое поколение
Спикеры: Беркин А., Дмитрук С. | ФСТ | 02.2026

## Инструмент «солнышко»

По словам Светланы, солнышко — это визуальная схема...
```

Чанки формируются по H2 как обычно. Оба имени в шапке попадают в embedding.

### Линейка — адаптация шапки и структуры

Каждый участник получает H2-раздел в лонгриде. Chunker разбивает по H2 автоматически. Шапка адаптируется для каждого чанка:

```
Тема: Спонсор, за которым идут
Участник: Беркин Андрей | Линейка, ведущий: Дмитрук С. | ФСТ | 02.2026

## Путь в бизнес (Беркин Андрей)

Я пришёл в бизнес 3 года назад. Светлана была моим спонсором...
```

**Ключевое отличие:** поле `Спикер` заменяется на `Участник` + `ведущий`. Это даёт:
- Поиск по конкретному участнику ("что рассказывал Беркин")
- Связь с ведущим линейки ("кто из команды Дмитрук рассказывал")
- Связь с общей темой ("примеры пути в бизнес")

Если H2-раздел участника превышает 600 слов — разбивается по параграфам как обычно. Возможно использование H3 внутри раздела участника для более гранулярного разбиения.

### Q&A — отдельный чанк

Q&A-раздел формируется как отдельный H2 в лонгриде → попадает в отдельный чанк автоматически. Формат: **Вопрос:** / **Ответ:** без указания кто спрашивал. Промпт инструктирует Claude убрать `SpeakerN` из текста при формировании Q&A-раздела.

```
Тема: Работа с возражениями
Спикер: Дмитрук С. | ФСТ | 02.2026

## Вопросы и ответы

**Вопрос:** Что делать, если клиент говорит «мне не нужно»?
**Ответ:** В этом случае я обычно...

**Вопрос:** А если клиент уже пользовался продуктом?
**Ответ:** Это частый случай. Здесь важно...
```

Q&A-чанк получает стандартную шапку основного спикера. Специальной обработки в saver не требуется — Claude уже убрал `SpeakerN` при создании лонгрида.

### Ценность для RAG

| Запрос пользователя | Что находит | Как контекст помогает |
|---|---|---|
| "Что рассказывал Беркин на ФСТ?" | Чанки с "Беркин" в шапке | Имя участника в embedding |
| "Примеры пути в бизнес" | Чанки линейки по H2 и содержанию | Семантический поиск по тексту |
| "Кто из команды Дмитрук рассказывал истории?" | Чанки с "ведущий: Дмитрук" | Связь участник ↔ ведущий |
| "Как работать с возражениями?" | Основные чанки + Q&A | Обычный семантический поиск |
| "Какие вопросы задавали на теме про возражения?" | Чанк Q&A | H2 "Вопросы и ответы" |

### `metadata.speaker` в transcript_chunks.json

Поле `materials[].metadata.speaker` (контракт BZ2-Bot v1.0) остаётся строкой. Значение зависит от сценария:

| Сценарий | `metadata.speaker` | Обоснование |
|----------|--------------------|-------------|
| Односпикерный | `"Иванова А."` | Как сейчас |
| Со-спикеры | `"Беркин А., Дмитрук С."` | Оба в одной строке |
| Линейка | `"Дмитрук С."` (ведущий) | Из имени файла, основной спикер |
| + Q&A | Не влияет | Анонимные не попадают в metadata |

Контракт BZ2-Bot не нарушается — `speaker` остаётся произвольной строкой, отображаемой в админке.

### Совместимость с H2 chunker

Текущий детерминистический chunker (H2-парсинг) **не меняется**. Изменения только в saver — формирование шапки чанка:

- **Со-спикеры:** `Спикеры: {имя1}, {имя2}` вместо `Спикер: {имя}`
- **Линейка:** `Участник: {имя} | Линейка, ведущий: {ведущий}` — шапка per-chunk, зависит от H2
- **Q&A:** Специальной обработки не требуется — Claude убирает `SpeakerN` на этапе лонгрида

Для линейки saver определяет участника H2-раздела по имени в скобках заголовка: `## Путь в бизнес (Беркин Андрей)` → regex `\(([^)]+)\)$` → "Беркин Андрей".

### Линейка: разбиение >600 слов

Если H2-раздел участника линейки превышает 600 слов, chunker разбивает по параграфам. Все подчанки сохраняют **тот же `topic`**, saver добавляет суффикс `(N/M)`. Per-chunk шапка **одинакова** для всех подчанков одного участника:

```
Тема: Спонсор, за которым идут
Участник: Беркин Андрей | Линейка, ведущий: Дмитрук С. | ФСТ | 02.2026

## Путь в бизнес (Беркин Андрей) (1/2)

[первая часть...]
```

```
Тема: Спонсор, за которым идут
Участник: Беркин Андрей | Линейка, ведущий: Дмитрук С. | ФСТ | 02.2026

## Путь в бизнес (Беркин Андрей) (2/2)

[вторая часть...]
```

Это корректно работает без изменений в chunker — topic сохраняется, saver парсит имя из topic и строит шапку.

---

## Ограничения

| Параметр | Лимит | Обоснование |
|----------|-------|-------------|
| Только MD-транскрипты | Whisper не поддерживает diarization | Метки спикеров — только из MacWhisper |
| SpeakerN = анонимный | Дефолт MacWhisper | Пользователь не подставляет имена для вопросов из зала |
| Паттерн имени | `Фамилия Имя` (кириллица/латиница) | Одинаковый формат MacWhisper |
| Diarization | Не реализуется | Внешний инструмент (MacWhisper) |

---

## Риски для односпикерной обработки

Мультиспикерные инструкции добавляются в существующие промпты — они **всегда видны Claude**, даже для тем с одним спикером. Ниже — конкретные риски и обязательные проверки.

| # | Риск | Митигация | Проверка |
|---|------|-----------|----------|
| 1 | **Промпт-блоат:** Claude видит секцию "Мультиспикерный контент" и начинает менять поведение для односпикерных тем (напр. менее уверенно пишет от первого лица) | Явная условная формулировка: *"Применяй ТОЛЬКО если в задании есть блок «Мультиспикерный контент». Без него — игнорируй секцию полностью"* | A/B тест: прогнать 2-3 реальных односпикерных транскрипта до и после добавления секции, сравнить лонгриды |
| 2 | **Map-reduce путь:** speaker context добавлен только в single-pass, map-reduce не получает информацию о спикерах | Добавить контекст в **оба** пути longread generator | Code review: проверить `_build_single_pass_prompt()` и промпты map-reduce |
| 3 | **Saver regex false positive:** `\(([^)]+)\)$` матчит обычные H2 типа `## Тема (практика)` | Regex применяется **только** при `scenario` содержащем `lineup` | Unit-тест: H2 с скобками при scenario=single не триггерит regex |
| 4 | **"Реплики ведущих → убрать" ослабление:** нюанс "кроме со-спикеров" может сделать Claude мягче для обычных тем | Условие привязано к блоку контекста в user prompt, не к базовой инструкции | A/B тест из п.1 покрывает |

---

## 7. План реализации

### Этап 0: Инфраструктура — ГОТОВО (v0.64)

- [x] `SpeakerInfo` модель в `schemas.py`
- [x] `speaker_utils.py` — `parse_speakers()` + self-tests
- [x] `VideoMetadata.speaker_info` — заполняется в `step_routes.py`
- [x] Clean prompt — сохранение меток спикеров

### Этап 1: Контекст спикеров в stages

- [ ] Формировать блок контекста мультиспикерности для user prompt из `speaker_info`
- [ ] Интегрировать контекст в Longread/Summary/Story stages

> Clean не нужен — с v0.72 это только глоссарная подстановка, не принимает решений о контенте.

### Этап 2: Адаптация промптов

Стратегия: условные секции в существующих файлах (не отдельные файлы).

- [ ] Добавить секцию "Мультиспикерный контент" в `longread/instructions.md`
- [ ] Добавить секцию "Мультиспикерный контент" в `summary/instructions.md`
- [ ] Добавить секцию "Мультиспикерный контент" в `story/instructions.md`

> ~~`cleaning/system.md`~~ — не требуется (v0.72+, см. §4.1).

### Этап 3: Чанки и контекстуализация

H2 chunker не меняется. Изменения только в saver (`_save_chunks_json`).

- [ ] Адаптировать формирование шапки чанка в saver по `metadata.speaker_info`
- [ ] Со-спикеры: `Спикеры: {имя1}, {имя2}` в шапке
- [ ] Линейка: `Участник: {имя} | Линейка, ведущий: {ведущий}` — per-chunk шапка
- [ ] Regex извлечения имени участника из topic: `\(([^)]+)\)$`
- [ ] `metadata.speaker` в JSON: со-спикеры через запятую, линейка — ведущий

### Этап 4: Тестирование и итерация

- [ ] Тестирование на реальных мультиспикерных транскриптах (все 3 сценария)
- [ ] Проверка разбиения >600 слов для линейки (per-chunk шапка + суффикс)
- [ ] Анализ качества longread/summary для каждого сценария
- [ ] Корректировка промптов по результатам

---

## 8. Тестирование

| Сценарий | Ожидаемый результат |
|----------|---------------------|
| MD с 1 спикером | `speaker_info.scenario == "single"`, промпты без изменений |
| MD с 2 спикерами | `scenario == "co_speakers"`, атрибуция в лонгриде |
| MD с 4 спикерами | `scenario == "lineup"`, H2-разделы по участникам |
| MD с 1 спикером + SpeakerN | `scenario == "qa"`, раздел Q&A |
| MD с 3 спикерами + SpeakerN | `scenario == "lineup_qa"` |
| Не-MD файл | `speaker_info is None`, pipeline без изменений |
| Clean — метки спикеров | Имена сохранены после глоссарной подстановки |
| Clean — SpeakerN | Сохранён для передачи в longread |
| Longread — линейка | H2 формат: `## Тема (Имя Фамилия)` для каждого участника |
| Longread — Q&A | `## Вопросы и ответы`, формат **Вопрос:**/**Ответ:** без SpeakerN |
| Summary — со-спикеры | Цитаты с атрибуцией |
| Summary — линейка | Мини-конспект по каждому участнику (подразделы) |
| Чанк — со-спикеры | Шапка: `Спикеры: Имя1, Имя2`, metadata.speaker: "Имя1, Имя2" |
| Чанк — линейка | Per-chunk шапка: `Участник: {имя}`, metadata.speaker: ведущий |
| Чанк — линейка >600 слов | Per-chunk шапка сохраняется + суффикс (1/2) в H2 |
| Чанк — Q&A | Стандартная шапка, текст без SpeakerN |
| RAG — поиск по участнику | Находит чанки конкретного участника линейки |

---

## Решённые вопросы

- [x] **Определение сценария** — программно, по количеству именованных спикеров и наличию SpeakerN
- [x] **Источник мультиспикерности** — только MD-транскрипты (MacWhisper с diarization)
- [x] **Анонимные участники** — `SpeakerN` (дефолт MacWhisper, имена не подставляются)
- [x] **Регулярные мероприятия** — при мультиспикерной теме пользователь вручную делает MD-транскрипт
- [x] **Summary для линейки** — мини-конспект по каждому участнику как подраздел общего конспекта
- [x] **Чанки** — адаптация шапки и структуры под сценарий, единый процесс с промптами
- [x] **Data flow** — `SpeakerInfo` хранится в `VideoMetadata.speaker_info`, доступна в stages и saver
- [x] **Стратегия промптов** — условные секции в существующих файлах, контекст сценария — в user prompt (template variables)
- [x] **Q&A формат** — **Вопрос:**/**Ответ:** без атрибуции, SpeakerN убирается промптом на этапе лонгрида
- [x] **metadata.speaker** — со-спикеры через запятую, линейка — ведущий (из имени файла)
- [x] **H2 chunker** — не меняется, всё решается на уровне промптов и saver
- [x] **Линейка >600 слов** — per-chunk шапка сохраняется при разбиении, суффикс (N/M) добавляется к H2

## Открытые вопросы

Нет открытых вопросов.

---

## История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2026-02-19 | 1.0 | Первоначальная версия |
| 2026-02-19 | 1.1 | Уточнение: только MD-транскрипты, программное определение сценария, SpeakerN |
| 2026-02-19 | 1.2 | Добавлены чанки и контекстуализация для RAG, summary для линейки |
| 2026-02-19 | 1.3 | Ревью архитектуры: data flow SpeakerInfo → VideoMetadata, единые промпты с условными секциями, Q&A без атрибуции, metadata.speaker по сценариям, пример >600 слов |
| 2026-02-19 | 1.4 | Перенос инфраструктуры (SpeakerInfo, speaker_utils, clean prompt) в v0.64; обновлён план v0.65 |
| 2026-02-22 | 1.5 | Ревью после v0.72+: §4.1 Clean упразднён (Clean = только глоссарий), "реплики ведущих" → в Longread, убраны привязки к v0.65, ссылка на архив md-transcript-import |
| 2026-02-22 | 1.6 | Добавлена секция "Риски для односпикерной обработки" — промпт-блоат, map-reduce путь, saver regex, ослабление инструкций |

---

_Документ для планирования в Claude Code_
