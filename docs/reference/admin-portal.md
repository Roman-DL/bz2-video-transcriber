# Админ-портал

> Веб-интерфейс для управления Базой Знаний: материалы, индексация, пользователи, аналитика поиска

**Статус:** Draft
**Дата:** 2026-02-07
**Серия:** [ROADMAP.md](ROADMAP.md) — требование #5

---

## 1. Проблема

На MVP управление материалами и индексацией — через Streamlit UI (инструмент разработчика, localhost). Управление пользователями — через Telegram-бот (/users, /pending, /block). Аналитики поиска нет.

**Текущая ситуация (к моменту MVP):**
- **Streamlit** (`scripts/admin_ui.py`) — CRUD материалов, просмотр чанков, индексация. Работает, но это localhost-скрипт без авторизации
- **CLI** (`scripts/index_content.py`) — batch-операции (import, reindex-all)
- **Telegram-бот** — модерация регистраций (/pending, approve/reject), управление пользователями (/users, /block)
- **search_logs** — данные копятся, но нет интерфейса для анализа

**Почему нужен портал:**
- **Делегирование** — когда появляется второй администратор (не разработчик), ему нужен нормальный UI, а не Streamlit на localhost
- **Аналитика** — search_logs без дашбордов бесполезны. Нужно видеть: популярные запросы, неудачные поиски, покрытие тем
- **Единая точка управления** — сейчас разбросано: Streamlit (материалы) + бот (пользователи) + DB-клиент (аналитика). Портал объединяет всё
- **UX модерации** — модерация регистраций через inline-кнопки в Telegram ограничена: нет фильтров, нет bulk-операций, нет истории

**Что было в n8n-версии:**
- Управление через Google Sheets (материалы, пользователи)
- Нет аналитики поиска
- Нет веб-интерфейса

---

## 2. Решение

### Концепция

Веб-приложение для администраторов БЗ 2.0. Работает поверх тех же сервисных функций, что Streamlit UI и CLI — **бэкенд не переписывается**, меняется только интерфейс.

```
Streamlit (MVP, разработчик)     Admin Portal (post-MVP, администраторы)
scripts/admin_ui.py              web-приложение с авторизацией
        │                                  │
        └──────────┬───────────────────────┘
                   ▼
        Общий бэкенд (src/):
        ├── content/services/indexer.py
        ├── content/services/materials.py     (CRUD)
        ├── content/services/embedding_provider.py
        ├── content/services/ai_assistant.py  (AI-метаданные, уже реализован)
        ├── users/services/user_management.py
        └── users/services/registration.py
```

### Функции портала

| Раздел | Функции | Заменяет |
|--------|---------|----------|
| **Материалы** | CRUD, редактирование описаний, смена статуса, фильтры по разделу/типу/статусу | Streamlit UI |
| **Индексация** | Запуск для одного/всех материалов, просмотр чанков и embeddings, импорт JSON | Streamlit UI + CLI |
| **Пользователи** | Список, фильтры, модерация регистраций, блокировка, смена роли | Telegram-бот (/users, /pending, /block) |
| **Аналитика поиска** | Дашборд: популярные запросы, неудачные поиски, использование tier 1 vs tier 2, покрытие тем | Нет (новое) |
| **Аналитика пользователей** | Вовлечённость, динамика по дням, использование функций (навигация vs поиск), аномалии | Нет (новое) |
| **Стоимость LLM** | Токены, стоимость по моделям/операциям, динамика расходов, расход по пользователям | Нет (новое) |
| **Отладка поиска** | Ввод запроса → результаты hybrid_search с скорами → сравнение tier 1 и tier 2 | Streamlit (позже, при smart-search) |
| **Настройки** | Режимы поиска (вкл/выкл tier 1/tier 2, минимальная роль), maintenance mode, LLM-модель, rate limit | Нет (новое, runtime без рестарта) |
| **Системные логи** | Просмотр ошибок, фильтры по уровню/компоненту/дате, связь с уведомлениями | Нет (новое). Подробнее: [monitoring.md](monitoring.md) |

### Ключевые идеи

1. **Тот же бэкенд** — портал вызывает те же сервисные функции, что Streamlit и CLI. Логика не дублируется. Streamlit послужил прототипом для проверки бэкенда.

2. **API-first** — FastAPI backend с REST API. Фронтенд общается через API. Это позволяет в будущем подключить другие клиенты (мобильное приложение, автоматизации).

3. **Авторизация** — только для пользователей с ролью admin/tab_team. Telegram Login Widget (пользователь уже есть в users, проверяем роль).

4. **Поэтапное внедрение** — портал не обязан заменить всё сразу. Первый этап: материалы + индексация. Второй: пользователи. Третий: аналитика. Streamlit и бот-команды работают параллельно.

5. **Аналитика как ценность** — search_logs копятся с первого дня smart-search. К моменту портала будут данные за недели/месяцы. Первые дашборды покажут: что ищут, что не находят, какие темы не покрыты документами.

### Отличия от Streamlit UI

| Аспект | Streamlit | Админ-портал |
|--------|-----------|-------------|
| Аудитория | Разработчик (я) | Администраторы (не-разработчики) |
| Доступ | localhost | Веб (с авторизацией) |
| Авторизация | Нет | Telegram Login Widget + проверка роли |
| UX | Минимальный, функциональный | Продуманный, с навигацией и фильтрами |
| Пользователи | Нет | Полное управление |
| Аналитика | Нет | Дашборды по search_logs |
| Многопользовательский | Нет (один процесс) | Да |

---

## 3. Пользовательские сценарии

### Сценарий A: Добавление нового материала (AI-assisted)

```
1. Админ открывает портал → раздел "Материалы" → кнопка "Добавить"
2. Вводит URL Google Drive → нажимает "Анализировать"
3. AI анализирует содержимое документа и предлагает:
   - Название (из заголовка документа)
   - Раздел (на основе содержимого + дерево разделов)
   - Описание (краткое описание с ключевыми терминами для поиска)
   - Тип (по MIME-type)
4. Админ проверяет предложения, корректирует при необходимости
5. Сохранить → материал создан (status=pending)
6. Кнопка "Индексировать" → pipeline создаёт embeddings
7. Просмотр чанков → проверка что описание/чанки адекватные
8. Кнопка "Активировать" → материал доступен в поиске
```

> **AI-ассистент** — подсказки, не автоматика. Админ всегда ревьюит и может изменить любое поле. Для не-текстовых материалов (PDF, картинки) описание генерируется из названия + контекста раздела (менее точно, чем для Google Docs с полным текстом).

### Сценарий B: Модерация регистраций

```
1. Админ открывает "Пользователи" → фильтр "Ожидают модерации"
2. Видит список заявок с данными: ФИО, TAB Team, Herbalife ID
3. Для каждой заявки: "Утвердить" или "Отклонить" (с причиной)
4. Bulk: выделить несколько → "Утвердить выбранные"
5. Пользователь получает уведомление в Telegram-боте
```

### Сценарий C: Анализ поисковых запросов

```
1. Админ открывает "Аналитика"
2. Дашборд показывает за последнюю неделю:
   - Топ-10 популярных запросов
   - Запросы без результатов (что не находят пользователи)
   - Соотношение: файловый поиск / Q&A tier 1 / Q&A tier 2
   - Частота автофоллбэка (tier 1 пуст → tier 2)
3. Вывод: "Много запросов про промоушен — нет документа" → добавить материал
```

### Сценарий E: Аналитика пользователей и динамика

```
1. Админ открывает "Аналитика" → вкладка "Пользователи"
2. График динамики: количество действий по дням (линии: поиск, навигация, файлы)
   → Видно тренды: рост/спад использования, пики активности
3. Метрики за период:
   - Активных пользователей / всего зарегистрированных
   - Использование функций: навигация vs поиск vs доставка файлов (%)
   - Топ активных пользователей (кол-во запросов, файлов)
   - Неактивные пользователи (>14 дней, >30 дней без действий)
4. Секция "Аномалии": необычная активность (500+ запросов/день)
5. Вывод: "65% пользователей используют поиск, рост 15% за месяц"
```

### Сценарий F: Мониторинг стоимости LLM

```
1. Админ открывает "Аналитика" → вкладка "Стоимость LLM"
2. Общие метрики за период: стоимость ($), токены, количество запросов
3. График стоимости по дням → видно динамику расходов
4. Breakdown по модели (Claude, OpenAI Embed, Whisper) и по операции (Q&A, embeddings, голос)
5. Топ пользователей по расходам → кто генерирует основную нагрузку
6. Вывод: "Средняя стоимость $0.42/день, 80% — Q&A генерация"
   → Понимание unit-экономики: сколько стоит обслуживание одного пользователя
```

### Сценарий I: Просмотр системных логов

```
1. Админ открывает "Логи"
2. Видит таблицу с последними записями:
   - 🔴 CRITICAL | database | "БД недоступна" | 15:30 | Уведомлено ✓
   - 🟠 ERROR | search | "Claude API: unauthorized" | 16:00 | Уведомлено ✓
   - 🟡 WARNING | search | "Rate limit exceeded" | 17:15 | —
3. Фильтры: уровень, компонент, период
4. Кнопка "Очистить старые" → удаляет записи старше 30 дней
5. Столбец "🔔" показывает, было ли отправлено Telegram-уведомление
```

> **Подробнее:** полная архитектура уведомлений, severity, rate limiting, таблица `system_logs` — см. [monitoring.md](monitoring.md)

### Сценарий D: Отладка поиска

```
1. Админ открывает "Отладка поиска"
2. Вводит запрос: "Сколько белка в Формуле 1?"
3. Видит результаты:
   - Intent: Q&A
   - Tier 1: 3 чанка (scores: 0.87, 0.72, 0.65)
   - Tier 2: 2 чанка (scores: 0.81, 0.58)
   - Материалы-источники для каждого чанка
4. Может переключить фильтры (chunk_type, trust_tier)
5. Понимает: tier 1 даёт хороший результат, tier 2 — дубликат
```

### Сценарий G: Настройка режимов поиска

```
1. Админ открывает "Настройки"
2. Видит текущее состояние всех режимов:
   - Файловый поиск: ✅ вкл, доступен всем (consultant+)
   - Q&A tier 1 (документы): ✅ вкл, доступен всем
   - Q&A tier 2 (транскрипты): ✅ вкл, доступен tab_team+
   - Автофоллбэк tier 1 → tier 2: ✅ вкл
   - Maintenance mode: ❌ выкл
3. Админ решает провести реиндексацию → включает maintenance mode
4. Вводит сообщение: "Проводятся технические работы, ~30 мин"
5. Сохранить → бот мгновенно начинает показывать сообщение вместо поиска
6. После работ → выключает maintenance mode → поиск восстановлен
```

### Сценарий H: Ограничение доступа к Q&A tier 2

```
1. Админ хочет протестировать tier 2 только с TAB Team перед открытием всем
2. Открывает "Настройки" → Q&A tier 2 → минимальная роль: tab_team
3. Сохранить → консультанты видят только tier 1, TAB Team — оба tier
4. После тестирования: минимальная роль → consultant → tier 2 открыт для всех
```

---

## 4. UI (высокоуровневые экраны)

### Навигация

```
┌───────────────────────────────────────────────────────┐
│  БЗ 2.0 — Админ-портал          [Роман ▾] [Выйти]     │
├──────────┬────────────────────────────────────────────┤
│          │                                            │
│ 📦 Мате- │  [Содержимое выбранного раздела]           │
│    риалы │                                            │
│ 🔍 Поиск │                                            │
│ 👥 Поль- │                                            │
│    зова- │                                            │
│    тели  │                                            │
│ 📊 Ана-  │                                            │
│    литика│                                            │
│ ⚙️ На-    │                                            │
│    стройки│                                           │
│          │                                            │
└──────────┴────────────────────────────────────────────┘
```

### Материалы — список

```
┌────────────────────────────────────────────────────────────────────┐
│ Материалы                                    [+ Добавить материал] │
├────────────────────────────────────────────────────────────────────┤
│ Фильтры: [Все разделы ▾] [Все типы ▾] [Все статусы ▾] [🔍 Поиск].  │
├────────────────────────────────────────────────────────────────────┤
│ # │ Название              │ Тип      │ Раздел    │ Статус  │ Idx   │
│───┼───────────────────────┼──────────┼───────────┼─────────┼───────│
│ 1 │ Формула 1             │ document │ Продукция │ active  │ ✓     │
│ 2 │ Каталог 2026          │ pdf      │ Продукция │ active  │ ✓     │
│ 3 │ Значок Супервайзера   │ image    │ Значки    │ pending │ ✗     │
│ 4 │ ПШ Полная оценка      │ video    │ Обучение  │ active  │ ✓     │
├────────────────────────────────────────────────────────────────────┤
│ Всего: 42 │ Active: 35 │ Pending: 5 │ Archive: 2                   │
└────────────────────────────────────────────────────────────────────┘
```

### Материал — карточка

```
┌────────────────────────────────────────────────────────────────────┐
│ ← Назад к списку                                                   │
├────────────────────────────────────────────────────────────────────┤
│ Формула 1                                        Статус: [active]  │
│                                                                    │
│ Тип: document    Раздел: Продукция    Доступ: level 1              │
│ URL: https://docs.google.com/...                                   │
│                                                                    │
│ Описание:                                                          │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ Формула 1 — основной продукт для сбалансированного питания.    │ │
│ │ Содержит 24г белка на порцию, витамины и минералы...           │ │
│ └────────────────────────────────────────────────────────────────┘ │
│                                                                    │
│ [💾 Сохранить] [🔄 Переиндексировать] [📁 Архивировать]            │
├────────────────────────────────────────────────────────────────────┤
│ Чанки (12)                                      Индексирован: ✓    │
│                                                                    │
│ ▸ #0 (material) — "Формула 1 — основной продукт..."                │
│ ▸ #1 (content, tier 1) — "Состав: сывороточный белок, казеин..."   │
│ ▸ #2 (content, tier 1) — "Способ приготовления: 2 мерные ложки..." │
│ ...                                                                │
└────────────────────────────────────────────────────────────────────┘
```

### Аналитика поиска

```
┌────────────────────────────────────────────────────────────────────┐
│ Аналитика поиска                     Период: [Последняя неделя ▾]  │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  Запросов: 342    Файловых: 128    Q&A: 214    Без результатов: 23 │
│                                                                    │
├──────────────────────────┬─────────────────────────────────────────┤
│ Топ запросов             │ Запросы без результатов                 │
│                          │                                         │
│ 1. формула 1 (24)        │ 1. промоушен январь (8)                 │
│ 2. значок (18)           │ 2. скидки для партнёров (5)             │
│ 3. каталог (15)          │ 3. условия возврата (4)                 │
│ 4. белок (12)            │ 4. сертификат качества (3)              │
│ 5. полная оценка (11)    │ 5. новинки 2026 (3)                     │
├──────────────────────────┴─────────────────────────────────────────┤
│ Автофоллбэк tier 1 → tier 2: 34% запросов (73 из 214 Q&A)          │
│ → Темы без документов: полная оценка, бизнес-план, мотивация       │
└────────────────────────────────────────────────────────────────────┘
```

### Аналитика пользователей

```
┌────────────────────────────────────────────────────────────────────┐
│ Аналитика пользователей                  Период: [Последний месяц ▾] │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  Активных: 156/342    Новых: 23     Среднее действий/день: 4.2     │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│ Динамика использования                                             │
│                                                                    │
│  60│        ╱╲                                                     │
│  40│   ╱╲  ╱  ╲  ╱╲                                                │
│  20│──╱──╲╱────╲╱──╲──                                             │
│   0└───┬───┬───┬───┬───┬───┬──                                     │
│     пн  вт  ср  чт  пт  сб  вс                                     │
│                                                                    │
│  ── Поиск  ── Навигация  ── Файлы                                  │
├──────────────────────────┬─────────────────────────────────────────┤
│ Использование функций    │ Неактивные пользователи                 │
│                          │                                         │
│ 🔍 Поиск:      45%      │ > 30 дней: 12 пользователей              │
│ 📂 Навигация:  38%      │ > 14 дней: 28 пользователей              │
│ 📁 Файлы:     17%       │                                          │
├──────────────────────────┴─────────────────────────────────────────┤
│ Топ активных пользователей                                         │
│                                                                    │
│ 1. Мария П. (TAB: Алматы) — 89 запросов, 45 файлов                 │
│ 2. Иван С. (TAB: Москва) — 67 запросов, 23 файла                   │
│ 3. ...                                                             │
├────────────────────────────────────────────────────────────────────┤
│ ⚠️ Аномалии                                                         │
│ • Пользователь X: 500+ запросов за день                            │
│ • Пользователь Y: 200+ загрузок файлов за час                      │
└────────────────────────────────────────────────────────────────────┘
```

### Стоимость LLM

```
┌────────────────────────────────────────────────────────────────────┐
│ Использование LLM                        Период: [Последний месяц ▾] │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  Общая стоимость: $12.45     Токенов: 2.1M     Запросов: 1,247     │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│ Стоимость по дням                                                  │
│                                                                    │
│  $1│     ╱╲                                                        │
│   0└──┬──┬──┬──┬──┬──┬──                                           │
│     пн вт ср чт пт сб вс                                           │
├──────────────────────────┬─────────────────────────────────────────┤
│ По модели                │ По операции                             │
│                          │                                         │
│ Claude Sonnet: $8.20     │ Q&A генерация:    $8.20  (66%)          │
│ OpenAI Embed:  $3.15     │ Embeddings:       $3.15  (25%)          │
│ Whisper:       $1.10     │ Whisper:          $1.10  (9%)           │
│                          │ AI-ассистент:     $0.00  (0%)           │
├──────────────────────────┴─────────────────────────────────────────┤
│ Топ по расходам (пользователи)                                     │
│                                                                    │
│ 1. Мария П. — $1.85 (148 Q&A запросов)                             │
│ 2. Иван С. — $1.20 (96 Q&A запросов)                               │
│ 3. ...                                                             │
└────────────────────────────────────────────────────────────────────┘
```

### Системные логи

```
┌────────────────────────────────────────────────────────────────────┐
│ Системные логи                                                     │
├────────────────────────────────────────────────────────────────────┤
│ Фильтры: [Все уровни ▾] [Все компоненты ▾] [Последние 24 часа ▾]  │
│                                                    [🗑 Очистить]   │
├────────────────────────────────────────────────────────────────────┤
│ # │ Время              │ Уровень  │ Компонент │ Сообщение │ 🔔   │
│───┼────────────────────┼──────────┼───────────┼───────────┼──────│
│ 1 │ 07.02 15:30        │ CRITICAL │ database  │ БД недос… │ ✓    │
│ 2 │ 07.02 16:00        │ ERROR    │ search    │ Claude …  │ ✓    │
│ 3 │ 07.02 17:15        │ WARNING  │ search    │ Rate li…  │ —    │
├────────────────────────────────────────────────────────────────────┤
│ Показано: 3 из 42 │ Страница 1 из 14                              │
└────────────────────────────────────────────────────────────────────┘
```

> Подробнее о таблице `system_logs`, severity, уведомлениях: [monitoring.md](monitoring.md)

### Настройки

```
┌────────────────────────────────────────────────────────────────────┐
│ Настройки системы                                                  │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│ ⚠️ Режим обслуживания                                               │
│ ┌────────────────────────────────────────────────────────────────┐ │
│ │ Maintenance mode: [❌ Выкл]                                    │ │
│ │ Сообщение: [Проводятся технические работы. Поиск временно      │ │
│ │            недоступен.                                    ]    │ │
│ └────────────────────────────────────────────────────────────────┘ │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│ Режимы поиска                                                      │
│                                                                    │
│ Файловый поиск:      [✅ Вкл]    Мин. роль: [consultant ▾]         │
│ Q&A tier 1 (докум.): [✅ Вкл]    Мин. роль: [consultant ▾]         │
│ Q&A tier 2 (транск.):[✅ Вкл]    Мин. роль: [consultant ▾]         │
│ Автофоллбэк tier 1→2:[✅ Вкл]                                      │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│ LLM                                                                │
│                                                                    │
│ Модель Q&A:    [claude-sonnet-4-5-20250929   ▾]                    │
│ Температура:   [0.3    ]                                           │
│ Результатов:   [5      ]  (match_count для hybrid_search)          │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│ Лимиты                                                             │
│                                                                    │
│ Запросов/час на пользователя: [60     ]                            │
│                                                                    │
├────────────────────────────────────────────────────────────────────┤
│                                                  [💾 Сохранить]    │
│                                                                    │
│ Последнее изменение: 07.02.2026, Роман (maintenance_mode → false)  │
└────────────────────────────────────────────────────────────────────┘
```

---

## 5. Данные

### Новые сущности

Портал работает с существующими таблицами + одна новая (`system_settings`):

| Таблица | Использование в портале |
|---------|------------------------|
| materials | CRUD, статус, описания |
| embeddings | Просмотр чанков, статистика индексации |
| sections | Фильтры, привязка материалов |
| users | Список, модерация, смена ролей |
| search_logs | Аналитика (создаётся в smart-search) |
| system_settings | Настройки режимов, feature flags, LLM-параметры (создаётся в content-pipeline) |
| system_logs | Просмотр ошибок, фильтрация (создаётся в content-pipeline, описание в [monitoring.md](monitoring.md)) |

### Таблица system_settings

**Таблица уже существует** (миграция в content-pipeline). Полная схема и seed defaults: [content-pipeline.md § system_settings](content-pipeline.md#этап-1-схема-бд--модель-миграция).

Портал предоставляет UI для управления настройками. Лог последнего изменения отображается в UI.

### API-слой

Портал общается с бэкендом через REST API. API переиспользует существующие сервисные функции:

```
GET    /api/materials              → content.services.materials.get_materials()
POST   /api/materials              → content.services.materials.create_material()
PUT    /api/materials/:id          → content.services.materials.update_material()
POST   /api/materials/:id/reindex  → content.services.indexer.index_material()
POST   /api/materials/:id/activate → content.services.indexer.activate_material()

GET    /api/users                  → users.services.user_management.get_users()
POST   /api/users/:id/approve      → users.services.registration.approve_user()
POST   /api/users/:id/block        → users.services.user_management.block_user()

GET    /api/analytics/search       → Агрегация search_logs (новый сервис)
POST   /api/search/debug           → Вызов hybrid_search() с возвратом скоров

GET    /api/settings               → Все настройки (system_settings)
PUT    /api/settings/:key          → Обновить настройку (value + updated_by)

POST   /api/auth/telegram          → Telegram Login Widget → проверка роли
```

---

## 6. Стек технологий

### Бэкенд: FastAPI

**Почему FastAPI:**
- Python (тот же язык, что и весь проект)
- Async (совместим с существующими async-сервисами и SQLAlchemy)
- Встроенная документация API (Swagger)
- Популярный, хорошая документация, много примеров
- Лёгкий — не тянет лишнего (в отличие от Django)

```python
# Пример: endpoint для материалов
@app.get("/api/materials")
async def list_materials(
    status: str | None = None,
    section_id: int | None = None,
    db: AsyncSession = Depends(get_db),
):
    return await get_materials(db, status=status, section_id=section_id)
```

### Фронтенд: определить позже

Варианты (в порядке простоты для соло-разработчика):

| Вариант | Плюсы | Минусы |
|---------|-------|--------|
| **Jinja2 + htmx** | Нет JS-фреймворка, серверный рендеринг, htmx добавляет интерактивность | Ограниченная интерактивность, сложнее для дашбордов |
| **React + admin-библиотека** (react-admin, Refine) | Богатый UI из коробки, готовые таблицы/фильтры/формы | Новый стек (React), сборка, TypeScript |
| **Vue.js + PrimeVue** | Проще React, хорошие компоненты | Новый стек (Vue), но проще |

> **Решение по фронтенду откладывается** до начала работы над порталом. К тому моменту будет опыт с Python и ясность, какой подход комфортнее. Streamlit покажет, какие UI-паттерны нужны.

### Авторизация: Telegram Login Widget

```
1. Пользователь нажимает "Войти через Telegram" на странице портала
2. Telegram Login Widget → callback с данными пользователя (id, name, hash)
3. Бэкенд: проверяет hash (Telegram Bot Token) → ищет user в БД → проверяет role
4. Если role = admin или tab_team → JWT-токен → доступ
5. Если нет → "Доступ запрещён"
```

Нет отдельной регистрации. Пользователь уже зарегистрирован через Telegram-бот.

### Деплой

Портал деплоится как отдельный процесс рядом с ботом. Общая БД.

```
┌──────────────┐  ┌──────────────────┐
│ Telegram Bot │  │ Admin Portal     │
│ (aiogram)    │  │ (FastAPI + front)│
│ :polling     │  │ :8080            │
└──────┬───────┘  └────────┬─────────┘
       │                   │
       └─────────┬─────────┘
                 ▼
       ┌──────────────────┐
       │ PostgreSQL       │
       │ + pgvector       │
       └──────────────────┘
```

---

## 7. Ограничения

| Параметр | Лимит | Обоснование |
|----------|-------|-------------|
| Пользователи портала | Только admin и tab_team роли | Портал не для консультантов |
| Авторизация | Telegram Login Widget | Нет отдельной системы учётных записей |
| Параллельная работа | Портал + бот + Streamlit — все могут работать одновременно | Общая БД, нет конфликтов (разные операции) |
| Аналитика | Только по search_logs | Не real-time; агрегация при запросе |

---

## 8. План реализации

### Зависимости

| Компонент | Требование | Статус зависимости |
|-----------|-----------|-------------------|
| CRUD материалов | [content-pipeline.md](content-pipeline.md) | Сервисные функции из Streamlit |
| Индексация | [content-pipeline.md](content-pipeline.md) | indexer.py |
| search_logs | [smart-search.md](smart-search.md) | Таблица + запись логов |
| User management | Уже реализовано (2.3) | users/services/ |
| hybrid_search() | [content-pipeline.md](content-pipeline.md) | SQL-функция |

Последовательность: content-pipeline → smart-search → **admin-portal**
Может идти **параллельно** с voice-commands.

### Этап 1: API + Материалы + Настройки

- [ ] FastAPI приложение (`src/admin/app.py`)
- [ ] Авторизация: Telegram Login Widget → JWT
- [ ] API endpoints: CRUD материалов, индексация, просмотр чанков
- [ ] API endpoints: GET/PUT настроек (`system_settings`)
- [ ] Фронтенд: страница материалов (список + карточка)
- [ ] Фронтенд: страница настроек (feature flags, maintenance, LLM, лимиты)
- [ ] Миграция Streamlit → портал для операций с материалами

### Этап 2: Пользователи

- [ ] API endpoints: список пользователей, фильтры, модерация, блокировка
- [ ] Фронтенд: страница пользователей
- [ ] Bulk-операции: утвердить/отклонить несколько заявок
- [ ] Модерация регистраций через портал (дополнение к Telegram)

### Этап 3: Аналитика + логи + отладка поиска

- [ ] Экран системных логов: таблица, фильтры по level/component/date, пагинация, столбец "Уведомлено" (см. [monitoring.md](monitoring.md))
- [ ] API: `GET /api/logs`, `GET /api/logs/stats`, `DELETE /api/logs?before={date}`
- [ ] Сервис агрегации search_logs
- [ ] API endpoints: топ запросов, неудачные поиски, tier-статистика
- [ ] Фронтенд: дашборд аналитики поиска
- [ ] Отладка поиска: ввод запроса → результаты с скорами + rules-based хинты
- [ ] Сервис агрегации user_events (таблица уже существует, миграция в smart-search) + search_logs → аналитика пользователей
- [ ] API endpoints: активность, использование функций, топ пользователей, неактивные, аномалии
- [ ] Фронтенд: дашборд аналитики пользователей (с графиком динамики)
- [ ] Сервис агрегации llm_usage_logs (таблица уже существует, миграция в content-pipeline) → стоимость по моделям, операциям, пользователям
- [ ] API endpoints: стоимость за период, timeseries, breakdown по модели/операции/пользователю
- [ ] Фронтенд: дашборд стоимости LLM (с графиком расходов по дням)

### Этап 4: AI-функции

- [x] ~~AI-ассистент при создании материалов (`suggest_material_metadata`)~~ — **реализовано в content-pipeline** ([content-pipeline.md](content-pipeline.md), этап 5)
- [x] ~~Промпт: анализ содержимого → title, section, description~~ — `src/content/services/ai_assistant.py`
- [x] ~~Интеграция с формой создания материала (кнопка "Анализировать")~~ — Streamlit UI (`scripts/admin_ui.py`)
- [ ] Интеграция `suggest_material_metadata` с REST API портала (переиспользовать существующий сервис)
- [ ] *Будущее:* AI-анализ search_logs → рекомендации по контенту (`generate_search_insights`)
- [ ] *Будущее:* AI-анализ user_events → выводы по вовлечённости (`generate_user_insights`)
- [ ] *Исследовать после использования:* AI-отладка поиска (оценка качества чанков, рекомендации)

---

## 9. Тестирование

| Сценарий | Ожидаемый результат |
|----------|---------------------|
| Авторизация admin через Telegram Login | Успешный вход, JWT, доступ ко всем разделам |
| Авторизация consultant через Telegram Login | Отказ: "Доступ запрещён" |
| CRUD материала через API | Создание, редактирование, индексация, активация |
| Модерация пользователя через портал | Утверждение → пользователь получает уведомление в Telegram |
| Аналитика за пустой период | Пустой дашборд, без ошибок |
| Отладка поиска | Результаты hybrid_search() с скорами, материалы-источники |
| Включение maintenance mode | Бот показывает сообщение вместо поиска, портал показывает статус |
| Выключение Q&A tier 2 | Пользователи не получают ответы из транскриптов, кнопка "Расширенный поиск" скрыта |
| Смена LLM-модели через настройки | Следующий Q&A запрос использует новую модель без рестарта |
| Изменение rate_limit | Лимит применяется мгновенно (после инвалидации кэша) |

---

## 10. Референсы

- [FastAPI](https://fastapi.tiangolo.com/) — Python web framework
- [Telegram Login Widget](https://core.telegram.org/widgets/login) — авторизация через Telegram
- [htmx](https://htmx.org/) — интерактивность без JS-фреймворка (вариант фронтенда)
- [react-admin](https://marmelab.com/react-admin/) — admin panel на React (вариант фронтенда)
- [docs/requirements/content-pipeline.md](content-pipeline.md) — бэкенд сервисы
- [docs/requirements/smart-search.md](smart-search.md) — search_logs для аналитики

---

## Требования к бэкенду (чеклист для реализации)

> **Как использовать:** при реализации content-pipeline и smart-search — сверяться с этим списком. Если сервисная функция нужна порталу — убедиться, что она поддерживает нужную сигнатуру. Отмечать `[x]` когда реализовано. Добавлять новые пункты по мере обсуждения портала.

### Материалы (content-pipeline)

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `get_materials(filters)` | Фильтры: status, section_id, material_type. Пагинация. Возвращать count | `content.services.materials` | [ ] |
| `get_material(id)` | Полная карточка + количество чанков + is_indexed | `content.services.materials` | [ ] |
| `create_material(...)` | Создание с валидацией. Возвращать созданный объект | `content.services.materials` | [ ] |
| `update_material(id, ...)` | Частичное обновление (title, description, status, section_id) | `content.services.materials` | [ ] |
| `get_chunks_for_material(id)` | Список чанков с chunk_type, chunk_text, metadata, trust_tier | `content.services.indexer` | [ ] |
| `get_index_stats()` | Breakdown: по chunk_type, trust_tier, status. Общие метрики | `content.services.indexer` | [ ] |
| `index_material(id)` | Возвращать IndexResult (chunks_created, errors) | `content.services.indexer` | [ ] |
| `activate_material(id)` | Проверка: is_indexed? Если нет — ошибка | `content.services.indexer` | [ ] |
| `get_sections_tree()` | Дерево разделов для фильтров и dropdown | `content.services.navigation` | [x] уже есть |
| `suggest_material_metadata(url)` | AI-анализ содержимого → предложить title, section, description, type | `content.services.ai_assistant` | [x] реализовано + Streamlit UI |

### Пользователи (уже реализовано, проверить сигнатуры)

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `get_users(filters)` | Фильтры: status, role, tab_team_id. Пагинация | `users.services.user_management` | [ ] проверить |
| `approve_user(id)` | Возвращать результат + отправлять уведомление в Telegram | `users.services.registration` | [x] есть, проверить return |
| `block_user(id, reason)` | Причина блокировки | `users.services.user_management` | [x] есть, проверить return |
| `get_pending_count()` | Для badge в навигации портала | `users.services.user_management` | [ ] |

### Поиск (smart-search)

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `hybrid_search(query, ...)` | Возвращать скоры (vector_rank, keyword_rank, rrf_score) — не только результаты | SQL-функция | [ ] |
| `detect_intent(query)` | Для отладки: показать какой intent определён | `rag.search` или `content.services` | [ ] |
| `aggregate_search_logs(period)` | Топ запросов, без результатов, tier-статистика, автофоллбэк % | Новый сервис | [ ] |
| Таблица `search_logs` | Поля: query, intent, tier_used, is_fallback, results_count, top_score | Миграция в smart-search | [ ] |

### Аналитика пользователей

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `aggregate_user_activity(period)` | Активных/всего, новых за период, средние действия/день | Новый сервис | [ ] |
| `get_feature_usage(period)` | Breakdown: навигация vs поиск vs доставка файлов (%) | Новый сервис (по search_logs + user_events) | [ ] |
| `get_top_users(period, limit)` | Топ пользователей по активности с breakdown по функциям | Новый сервис | [ ] |
| `get_inactive_users(days_threshold)` | Пользователи без действий > N дней | Новый сервис | [ ] |
| `detect_anomalies(period)` | Необычная активность: слишком много запросов/загрузок | Новый сервис | [ ] |
| `get_usage_timeseries(period, granularity)` | Количество действий по дням/неделям для графика динамики | Новый сервис (агрегация по created_at) | [ ] |
| Таблица `user_events` | **Уже существует** (миграция в [smart-search](smart-search.md)). Поля: user_id, event_type, metadata, created_at | — | — |

> **Источники данных:** `search_logs` и `user_events` — обе таблицы создаются в smart-search (миграция). `search_logs` — поиск. `user_events` — навигация, файлы, голос.

### Стоимость LLM

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `aggregate_llm_costs(period)` | Общие: стоимость, токены, кол-во запросов за период | Новый сервис | [ ] |
| `get_cost_timeseries(period, granularity)` | Стоимость по дням/неделям для графика | Новый сервис | [ ] |
| `get_cost_by_model(period)` | Breakdown по модели: Claude, OpenAI Embed, Whisper | Новый сервис | [ ] |
| `get_cost_by_operation(period)` | Breakdown по операции: Q&A, embeddings, whisper, AI-ассистент | Новый сервис | [ ] |
| `get_cost_by_user(period, limit)` | Топ пользователей по расходам | Новый сервис | [ ] |
| Таблица `llm_usage_logs` | **Уже существует** (миграция в [content-pipeline](content-pipeline.md)). Поля: user_id, operation, model, input_tokens, output_tokens, cost_usd, metadata, created_at | — | — |

> **Логирование:** каждый вызов LLM/embedding/Whisper API записывает строку в `llm_usage_logs`. `user_id` = NULL для системных операций (reindex, AI-ассистент). `cost_usd` рассчитывается по тарифам модели на момент вызова. Тарифы — в конфиге.
>
> **Cross-cutting:** логирование затрагивает все модули-потребители API: smart-search (Q&A генерация), content-pipeline (embeddings), voice-commands (Whisper), admin-portal (AI-ассистент). Каждый провайдер (LLM, embedding, whisper) отвечает за запись в `llm_usage_logs`.

### AI-функции (бэкенд)

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `suggest_material_metadata(url)` | AI-предложения при создании материала (см. Материалы выше) | `content.services.ai_assistant` | [x] реализовано |
| `generate_search_insights(period)` | AI-анализ search_logs → рекомендации по контенту (будущее) | Новый сервис | [ ] будущее |
| `generate_user_insights(period)` | AI-анализ user_events → выводы по вовлечённости (будущее) | Новый сервис | [ ] будущее |

### Настройки системы

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `get_all_settings()` | Все настройки для страницы "Настройки" | `common.services.settings` | [ ] |
| `get_setting(key)` | Одна настройка с кэшем (TTL 60 сек). Используется в search pipeline | `common.services.settings` | [ ] |
| `update_setting(key, value, updated_by)` | Обновить настройку + инвалидировать кэш | `common.services.settings` | [ ] |
| Таблица `system_settings` | **Уже существует** (миграция в [content-pipeline](content-pipeline.md)). key (PK), value (JSONB), updated_at, updated_by | — | — |

> **Сервис настроек** — в `src/common/services/settings.py`, т.к. используется всеми модулями (smart-search проверяет feature flags, LLM provider читает модель/температуру). Не привязан к конкретному домену.

### Системные логи ([monitoring.md](monitoring.md))

| Функция | Что нужно порталу | Где реализуется | Статус |
|---------|-------------------|-----------------|--------|
| `get_logs(filters)` | Фильтры: level, component, date_from, date_to. Пагинация | Новый сервис | [ ] |
| `get_log_stats(period)` | Количество по уровням за период | Новый сервис | [ ] |
| `delete_old_logs(before_date)` | Очистка записей старше указанной даты | Новый сервис | [ ] |
| Таблица `system_logs` | **Создаётся** в content-pipeline (миграция). Поля: level, component, message, context, notified, created_at | — | [ ] |

### Общие принципы для бэкенда

- **Сервисы возвращают данные, не форматируют** — никакого HTML/Telegram-разметки в return. Форматирование — в UI-слое (бот, Streamlit, портал)
- **Фильтрация через kwargs** — `get_materials(db, status=None, section_id=None, ...)`. Опциональные параметры, не обязательные
- **Результаты операций — dataclass/dict** — `IndexResult(chunks_created=12, errors=[])`, не просто `True/False`
- **Пагинация** — `limit` + `offset` в списковых функциях. Возвращать `total_count` для UI

---

## Принятые решения

| Вопрос | Решение | Обоснование |
|--------|---------|-------------|
| Streamlit vs Admin Portal | Оба. Streamlit = MVP прототип (валидация бэкенда). Портал = production UI | Streamlit проверяет логику, портал добавляет авторизацию, UX, аналитику |
| Отдельное приложение или часть бота? | Отдельный процесс (FastAPI) | Бот = aiogram polling, портал = HTTP server. Разные runtime, общая БД |
| Авторизация | Telegram Login Widget | Пользователь уже в БД. Нет отдельных учётных записей. Проверка роли |
| Фронтенд стек | Определить позже | Опыт с Streamlit покажет нужные UI-паттерны. Решение при начале работы |
| Модерация: бот или портал? | Оба (параллельно) | Бот — для быстрой реакции (push-уведомление). Портал — для bulk-операций и фильтров |
| AI-помощник при добавлении | Реализован в content-pipeline (этап 5), портал переиспользует | Описание — ключ к качеству файлового поиска. Сервис нужен и при индексации (автозаполнение), и в UI |
| AI-анализ search_logs | Отложить (будущее) | SQL-дашборд покрывает 80%. AI полезен когда накопятся данные за недели/месяцы |
| AI-отладка поиска | Исследовать позже | Rules-based хинты достаточны. AI-отладку исследовать после реального использования |
| Аналитика пользователей | Включить (этап 3) | Критично: вовлечённость, злоупотребления, эффективность БЗ. AI-выводы — будущее |
| Хранение настроек | Таблица `system_settings` (key-value, JSONB) | Runtime без рестарта. БД, а не config/env — для изменения через портал |
| Где создавать таблицу настроек | В content-pipeline (миграция) | smart-search проверяет настройки при каждом запросе — таблица нужна до поиска |
| Кэширование настроек | In-memory dict, TTL 60 сек | 1 SELECT при старте; не на каждый запрос. Инвалидация при записи через портал |

## Открытые вопросы

- [ ] Фронтенд стек: Jinja2 + htmx vs React vs Vue? Решить при начале работы над порталом
- [ ] Нужен ли real-time (WebSocket) для уведомлений в портале? Или polling достаточно?
- [ ] Доступ tab_team к порталу: все разделы или только их TAB Team пользователи?
- [x] ~~Нужна ли аналитика по пользователям (активность, частота использования)?~~ → **Да, важно.** Вовлечённость, использование функций (навигация vs поиск), аномалии, неактивные пользователи. Новая таблица `user_events` + агрегация. AI-анализ — после накопления данных

---

## История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2026-02-07 | 1.0 | Первоначальная версия: концепция, функции, UI-макеты, стек, этапы |
| 2026-02-07 | 1.1 | Добавлена секция «Требования к бэкенду» — чеклист для сверки при реализации content-pipeline и smart-search |
| 2026-02-07 | 1.2 | AI-функции: AI-ассистент при создании материалов (включить), AI-анализ аналитики (будущее), AI-отладка поиска (исследовать позже). Аналитика пользователей: вовлечённость, функции, аномалии. Таблица user_events. Этапы 3-4 расширены |
| 2026-02-07 | 1.3 | Динамика использования (график по дням). Стоимость LLM: таблица llm_usage_logs, дашборд расходов по моделям/операциям/пользователям. Сценарий F: мониторинг стоимости |
| 2026-02-07 | 1.4 | Настройки системы: таблица system_settings (key-value JSONB), feature flags (режимы поиска, роли, maintenance mode), LLM-параметры (модель, температура), rate limit. Сценарии G, H. UI-макет настроек. Бэкенд-чеклист. Кросс-ссылки в content-pipeline и smart-search |
| 2026-02-07 | 1.5 | Системные логи: сценарий I, UI-макет, бэкенд-чеклист, таблица system_logs. Кросс-ссылки с [monitoring.md](monitoring.md) |
| 2026-02-08 | 1.6 | AI-ассистент (suggest_material_metadata) реализован в content-pipeline, не в портале. Этап 4 обновлён: портал переиспользует через REST API |

---

_Документ для планирования в Claude Code_
