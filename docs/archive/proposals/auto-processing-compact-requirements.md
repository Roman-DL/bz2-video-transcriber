# Компактная форма автоматической обработки

> **Дата:** 2026-01-23
> **Статус:** ⏳ Планирование
> **Версия:** v0.54
> **Контекст:** Оптимизация UI формы автоматической обработки

---

## 1. Проблема

Текущая форма автоматической обработки занимает слишком много места по вертикали (~500px):

| Элемент | Проблема |
|---------|----------|
| Список этапов | Большие отступы между элементами |
| Метрики этапов | Занимают место, но бесполезны без возможности влияния |
| Список файлов | Выводится после завершения, удлиняет форму |
| Иконки | Избыточный размер (24px) |

**Цель:** Сократить высоту формы до ~280px, сохранив информативность.

---

## 2. Решение

### 2.1 Что убираем

| Элемент | Причина |
|---------|---------|
| Метрики этапов | Нельзя повлиять на результат в авто-режиме |
| Список созданных файлов | Всё есть в архиве |
| Большие отступы | Визуальный шум |

### 2.2 Что оставляем

| Элемент | Причина |
|---------|---------|
| Название файла | Понимать что обрабатывается |
| Текущий этап + прогресс | Понимать что происходит |
| Список всех этапов | Контроль процесса |
| Кнопка "Открыть в архиве" | Быстрый доступ к результату |

---

## 3. UI: Новый дизайн

### 3.1 Состояние "Выполняется"

```
┌─────────────────────────────────────────────────────────────────┐
│ АВТОМАТИЧЕСКАЯ ОБРАБОТКА                              Отменить │
├─────────────────────────────────────────────────────────────────┤
│ 2026.01 Форум Табтим. Тестовая тема (Светлана Дмитрук).mp3     │
├─────────────────────────────────────────────────────────────────┤
│  🔄 ВЫПОЛНЯЕТСЯ                                                 │
│  Извлечение слайдов (4/8)                         менее 5 сек  │
│  ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  33%   │
├─────────────────────────────────────────────────────────────────┤
│  ✓ Парсинг метаданных                                          │
│  ✓ Транскрипция (Whisper)                                      │
│  ✓ Очистка текста                                              │
│  ● Извлечение слайдов                                          │
│  ○ Генерация лонгрида                                          │
│  ○ Генерация конспекта                                         │
│  ○ Разбиение на чанки                                          │
│  ○ Сохранение в архив                                          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Состояние "Завершено"

```
┌─────────────────────────────────────────────────────────────────┐
│ АВТОМАТИЧЕСКАЯ ОБРАБОТКА                                        │
├─────────────────────────────────────────────────────────────────┤
│ 2026.01 Форум Табтим. Тестовая тема (Светлана Дмитрук).mp3     │
├─────────────────────────────────────────────────────────────────┤
│  ✅ УСПЕШНО ЗАВЕРШЕНО                                           │
│     Все этапы выполнены                                         │
├─────────────────────────────────────────────────────────────────┤
│  ✓ Парсинг метаданных                                          │
│  ✓ Транскрипция (Whisper)                                      │
│  ✓ Очистка текста                                              │
│  ✓ Извлечение слайдов                                          │
│  ✓ Генерация лонгрида                                          │
│  ✓ Генерация конспекта                                         │
│  ✓ Разбиение на чанки                                          │
│  ✓ Сохранение в архив                                          │
├─────────────────────────────────────────────────────────────────┤
│  [📂 Открыть в архиве]                                          │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 Состояние "Ошибка"

```
┌─────────────────────────────────────────────────────────────────┐
│ АВТОМАТИЧЕСКАЯ ОБРАБОТКА                                        │
├─────────────────────────────────────────────────────────────────┤
│ 2026.01 Форум Табтим. Тестовая тема (Светлана Дмитрук).mp3     │
├─────────────────────────────────────────────────────────────────┤
│  ❌ ОШИБКА                                                      │
│     Генерация лонгрида: API timeout                             │
├─────────────────────────────────────────────────────────────────┤
│  ✓ Парсинг метаданных                                          │
│  ✓ Транскрипция (Whisper)                                      │
│  ✓ Очистка текста                                              │
│  ✓ Извлечение слайдов                                          │
│  ✗ Генерация лонгрида                                          │
│  ○ Генерация конспекта                                         │
│  ○ Разбиение на чанки                                          │
│  ○ Сохранение в архив                                          │
├─────────────────────────────────────────────────────────────────┤
│  [🔄 Повторить]                              [Закрыть]          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. Спецификация компонентов

### 4.1 Структура

```
AutoProcessingModal/
├── Header              # Заголовок + кнопка отмены
├── FileName            # Название обрабатываемого файла
├── ProgressSection     # Статус + прогресс-бар
├── StepsList           # Список этапов
└── Footer              # Кнопки действий (при завершении)
```

### 4.2 ProgressSection

```typescript
interface ProgressSectionProps {
  status: 'running' | 'completed' | 'error';
  currentStep: number;       // 1-based
  totalSteps: number;
  stepLabel: string;         // "Извлечение слайдов"
  progress: number;          // 0-100
  timeRemaining?: string;    // "менее 5 сек"
  errorMessage?: string;     // При status === 'error'
}
```

**Отображение по статусу:**

| Статус | Иконка | Цвет | Текст |
|--------|--------|------|-------|
| running | 🔄 Spinner | blue-500 | "ВЫПОЛНЯЕТСЯ" |
| completed | ✅ CheckCircle | emerald-500 | "УСПЕШНО ЗАВЕРШЕНО" |
| error | ❌ AlertCircle | red-500 | "ОШИБКА" |

### 4.3 StepItem

```typescript
interface StepItemProps {
  label: string;
  status: 'pending' | 'running' | 'completed' | 'error';
  isLast: boolean;           // Для линии-коннектора
}
```

**Иконки статуса:**

| Статус | Иконка | Размер | Цвет |
|--------|--------|--------|------|
| pending | ○ Circle outline | 20px | stone-300 |
| running | 🔄 Spinner | 20px | blue-500 bg |
| completed | ✓ Check | 20px | emerald-500 bg |
| error | ✗ X | 20px | red-500 bg |

### 4.4 Footer

| Статус | Кнопки |
|--------|--------|
| running | — (нет footer) |
| completed | "Открыть в архиве" (primary) |
| error | "Повторить" (primary) + "Закрыть" (secondary) |

---

## 5. Размеры и отступы

### 5.1 Сравнение: было / стало

| Параметр | Было | Стало |
|----------|------|-------|
| Общая высота | ~500px | ~280px |
| Отступ между этапами | 12-16px | 6px |
| Иконка статуса | 24px | 20px |
| Шрифт этапа | 14px | 13px |
| Padding секций | 16-20px | 12px |

### 5.2 CSS-переменные

```css
:root {
  --modal-padding-x: 16px;
  --modal-padding-y: 12px;
  --step-gap: 6px;
  --step-icon-size: 20px;
  --step-font-size: 13px;
  --progress-bar-height: 6px;
}
```

---

## 6. Условное отображение

### 6.1 Шаг "Слайды"

Шаг "Извлечение слайдов" отображается только если к файлу прикреплены слайды:

```typescript
const steps = useMemo(() => {
  const base = [
    { id: 'parse', label: 'Парсинг метаданных' },
    { id: 'transcribe', label: 'Транскрипция (Whisper)' },
    { id: 'clean', label: 'Очистка текста' },
    // slides - условный
    { id: 'longread', label: 'Генерация лонгрида' },
    { id: 'summarize', label: 'Генерация конспекта' },
    { id: 'chunk', label: 'Разбиение на чанки' },
    { id: 'save', label: 'Сохранение в архив' },
  ];

  if (hasSlides) {
    const idx = base.findIndex(s => s.id === 'longread');
    base.splice(idx, 0, { id: 'slides', label: 'Извлечение слайдов' });
  }

  return base;
}, [hasSlides]);
```

### 6.2 Кнопка "Отменить"

Показывается только в статусе `running`.

### 6.3 Footer

Показывается только в статусах `completed` и `error`.

---

## 7. Анимации

### 7.1 Spinner

```css
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.spinner {
  animation: spin 1s linear infinite;
}
```

### 7.2 Progress bar

```css
.progress-bar {
  transition: width 300ms ease-out;
}
```

### 7.3 Появление этапа

```css
.step-item {
  transition: opacity 200ms, color 200ms;
}
```

---

## 8. Действия пользователя

| Действие | Результат |
|----------|-----------|
| "Отменить" | Прерывание обработки, закрытие модалки |
| "Открыть в архиве" | Переход к папке в архиве, закрытие модалки |
| "Повторить" | Перезапуск обработки с начала |
| "Закрыть" | Закрытие модалки без действий |
| Клик вне модалки | Ничего (модалка блокирующая) |

---

## 9. Интеграция

### 9.1 Props компонента

```typescript
interface AutoProcessingModalProps {
  // Данные
  fileName: string;
  hasSlides: boolean;
  
  // Состояние (из родителя или WebSocket)
  status: 'running' | 'completed' | 'error';
  currentStepId: string;
  progress: number;
  timeRemaining?: string;
  errorMessage?: string;
  
  // Callbacks
  onCancel: () => void;
  onRetry: () => void;
  onClose: () => void;
  onOpenArchive: (folderId: string) => void;
}
```

### 9.2 WebSocket events (без изменений)

```typescript
// Существующие события остаются
interface PipelineProgressEvent {
  type: 'pipeline:progress';
  step: string;
  progress: number;
  eta?: number;
}

interface PipelineCompleteEvent {
  type: 'pipeline:complete';
  folderId: string;
}

interface PipelineErrorEvent {
  type: 'pipeline:error';
  step: string;
  message: string;
}
```

---

## 10. Тестовые сценарии

| Сценарий | Проверка |
|----------|----------|
| Обработка без слайдов | 7 этапов в списке |
| Обработка со слайдами | 8 этапов в списке |
| Успешное завершение | Кнопка "Открыть в архиве" |
| Ошибка на этапе | Красная иконка + кнопки "Повторить"/"Закрыть" |
| Отмена во время обработки | Прерывание + закрытие |
| Длинное название файла | Truncate с title tooltip |

---

## 11. План реализации

### Задачи

- [ ] Создать компонент `ProgressSection` с тремя статусами
- [ ] Обновить компонент `StepItem` (уменьшить размеры)
- [ ] Обновить `AutoProcessingModal`:
  - [ ] Убрать метрики этапов
  - [ ] Убрать список файлов после завершения
  - [ ] Добавить footer с кнопками
  - [ ] Уменьшить отступы
- [ ] Добавить условный шаг "Слайды"
- [ ] Тестирование всех состояний

### Оценка

| Компонент | Время |
|-----------|-------|
| ProgressSection | 1ч |
| StepItem update | 0.5ч |
| AutoProcessingModal | 1.5ч |
| Тестирование | 1ч |
| **Итого** | **4ч** |

---

## 12. Референсы

### 12.1 Прототип

- `AutoProcessingCompact.jsx` — интерактивный прототип с демо

### 12.2 Текущая реализация

- Скриншоты текущего UI (в начале документа)

---

## История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2026-01-23 | 1.0 | Первоначальная версия требований |
