# План: Окно настроек для выбора моделей

## Цель
Реализовать окно настроек, позволяющее выбирать модели для каждого этапа обработки видео (транскрипция, очистка, чанкирование, суммаризация) с отображением параметров каждой модели.

## Архитектурные решения

| Вопрос | Решение |
|--------|---------|
| Где хранить настройки? | **localStorage** + React Context (персистентность между сессиями) |
| Как передавать модели? | Опциональное поле `model` в Request schemas |
| Backend API для настроек? | Только чтение: `/api/models/available`, `/api/models/default`, `/api/models/config` |
| Параметры моделей? | Из `config/models.yaml` + добавить `context_tokens` для каждой модели |

## Этапы реализации

### 1. Обновить config/models.yaml
**Изменить:** `config/models.yaml`

Добавить `context_tokens` для каждой модели:
```yaml
models:
  gemma2:
    context_tokens: 8192      # NEW
    cleaner:
      chunk_size: 3000
      # ...
  qwen2:
    context_tokens: 32768     # NEW
    # ...
  qwen3:
    context_tokens: 40960     # NEW
    # ...
```

### 2. Backend: API для моделей
**Новый файл:** `backend/app/api/models_routes.py`

```python
@router.get("/available")  # Список моделей с Ollama и Whisper
@router.get("/default")    # Текущие default модели из Settings
@router.get("/config")     # Параметры из models.yaml для всех моделей
```

Endpoint `/config` возвращает:
```json
{
  "gemma2": {
    "context_tokens": 8192,
    "cleaner": {"chunk_size": 3000, "chunk_overlap": 200, ...},
    "chunker": {...},
    "text_splitter": {...}
  },
  "qwen2": {...}
}
```

**Изменить:** `backend/app/main.py` — добавить router

### 3. Backend: Поддержка override моделей
**Изменить:** `backend/app/models/schemas.py`

Добавить опциональное поле в Request классы:
- `StepParseRequest` → `whisper_model: str | None = None`
- `StepCleanRequest` → `model: str | None = None`
- `StepChunkRequest` → `model: str | None = None`
- `StepSummarizeRequest` → `model: str | None = None`

**Изменить:** `backend/app/api/step_routes.py`
- Передавать model из request в orchestrator методы

**Изменить:** `backend/app/services/pipeline.py`
- Добавить параметр `model` в методы `clean()`, `chunk()`, `summarize()`

### 4. Frontend: API hooks
**Новый файл:** `frontend/src/api/hooks/useModels.ts`
- `useAvailableModels()` — список моделей
- `useDefaultModels()` — default модели
- `useModelsConfig()` — параметры моделей из models.yaml

**Изменить:** `frontend/src/api/types.ts`
- Добавить `model?: string` в Request types
- Добавить типы `ModelConfig`, `ModelsConfigResponse`

### 5. Frontend: Settings Context
**Новый файл:** `frontend/src/contexts/SettingsContext.tsx`
- `SettingsProvider` — загрузка/сохранение в localStorage
- `useSettings()` — хук для доступа к настройкам

### 6. Frontend: UI настроек
**Новый файл:** `frontend/src/components/settings/SettingsModal.tsx`
- 4 селектора (transcribe, clean, chunk, summarize)
- Whisper модели для transcribe, Ollama для остальных
- Кнопки: Сбросить / Отмена / Сохранить

**Изменить:** `frontend/src/components/layout/Header.tsx`
- Добавить кнопку ⚙️ для открытия настроек

### 7. Frontend: Интеграция
**Изменить:** `frontend/src/App.tsx`
- Обернуть в `SettingsProvider`

**Изменить:** `frontend/src/components/processing/StepByStep.tsx`
- Использовать `useSettings()` для получения моделей
- Передавать модели в API запросы

## Файлы для изменения

| Файл | Действие |
|------|----------|
| `config/models.yaml` | Изменить (добавить context_tokens) |
| `backend/app/api/models_routes.py` | Создать |
| `backend/app/main.py` | Изменить (добавить router) |
| `backend/app/models/schemas.py` | Изменить (добавить model поля) |
| `backend/app/api/step_routes.py` | Изменить (передавать model) |
| `backend/app/services/pipeline.py` | Изменить (параметры model) |
| `frontend/src/api/hooks/useModels.ts` | Создать |
| `frontend/src/api/types.ts` | Изменить (model поля, ModelConfig types) |
| `frontend/src/contexts/SettingsContext.tsx` | Создать |
| `frontend/src/components/settings/SettingsModal.tsx` | Создать |
| `frontend/src/components/layout/Header.tsx` | Изменить (кнопка) |
| `frontend/src/App.tsx` | Изменить (SettingsProvider) |
| `frontend/src/components/processing/StepByStep.tsx` | Изменить (useSettings) |

## UI Preview

```
┌─────────────────────────────────────────────────┐
│  БЗ2 Транскрибатор  v0.10.0    [●][●]   [⚙️]   │
└─────────────────────────────────────────────────┘
                                           ↓
┌──────────────────────────────────────────────────────┐
│   Настройки моделей                                  │
├──────────────────────────────────────────────────────┤
│ Транскрипция                                         │
│ [Systran/faster-whisper-large-v3              ▼]     │
│                                                      │
│ Очистка                                              │
│ [gemma2:9b                                    ▼]     │
│ ┌──────────────────────────────────────────────┐     │
│ │ 📊 Контекст: 8K токенов                      │     │
│ │ chunk_size: 3000 • overlap: 200              │     │
│ │ small_text_threshold: 3500                   │     │
│ └──────────────────────────────────────────────┘     │
│                                                      │
│ Чанкирование                                         │
│ [gemma2:9b                                    ▼]     │
│ ┌──────────────────────────────────────────────┐     │
│ │ 📊 Контекст: 8K токенов                      │     │
│ │ large_text_threshold: 10000                  │     │
│ │ min_chunk_words: 100 • target: 250           │     │
│ └──────────────────────────────────────────────┘     │
│                                                      │
│ Суммаризация                                         │
│ [qwen2.5:14b                                  ▼]     │
│ ┌──────────────────────────────────────────────┐     │
│ │ 📊 Контекст: 32K токенов                     │     │
│ └──────────────────────────────────────────────┘     │
│                                                      │
│ [Сбросить]                          [Отмена][Save]   │
└──────────────────────────────────────────────────────┘
```

**Примечания к UI:**
- Для каждой модели показывается блок с параметрами
- Контекст в токенах (из models.yaml)
- Параметры релевантные для этапа (cleaner → cleaner params, chunker → chunker params)
- Если параметры не настроены (модель не в models.yaml) — показать "Используются параметры по умолчанию"

## Верификация

1. **Backend API:**
   ```bash
   curl http://100.64.0.1:8801/api/models/available
   curl http://100.64.0.1:8801/api/models/default
   curl http://100.64.0.1:8801/api/models/config
   ```

2. **Frontend:**
   - Открыть настройки через кнопку в Header
   - Изменить модель для одного из этапов
   - Закрыть и открыть заново — настройки сохранились
   - Обновить страницу — настройки сохранились (localStorage)

3. **E2E:**
   - Выбрать другую модель для очистки (например qwen2.5:7b)
   - Запустить обработку видео
   - Проверить в логах backend что используется выбранная модель
