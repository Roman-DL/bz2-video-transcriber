# Фаза 8: Интеграция слайдов презентаций

> **Дата:** 2026-01-23
> **Статус:** ⏳ Планирование
> **Версии:** v0.50—v0.52 (план)
> **Контекст:** Повышение качества лонгрида за счёт данных со слайдов

---

## 1. Проблема

Спикер не всегда проговаривает информацию со слайдов презентации. При этом слайды могут содержать:

| Тип данных | Пример | Ценность для лонгрида |
|------------|--------|----------------------|
| **Точные цифры** | "Белок 18г — 36% РСП" | Конкретика вместо "много белка" |
| **Таблицы** | Сравнение продуктов | Структурированные данные |
| **Списки** | "5 шагов к результату" | Чёткая последовательность |
| **Цитаты** | "Продукт №1 — Euromonitor" | Авторитетные источники |
| **Схемы** | Воронка продаж | Визуальные концепции |

**Решение:** Извлекать текст и структуры со слайдов с помощью vision-модели и добавлять к контексту генерации лонгрида.

---

## 2. Концепция решения

### 2.1 Пользовательский сценарий

```
1. Пользователь на главном экране прикрепляет слайды к файлу
2. Запускает пошаговую обработку
3. После этапа "Очистка" появляется шаг "Слайды"
4. Vision-модель извлекает данные → результат показывается
5. Лонгрид генерируется с учётом cleaned + slides_extracted
```

### 2.2 Архитектура

```
                    ┌─────────────────────────────────────────┐
                    │           ГЛАВНЫЙ ЭКРАН                 │
                    │  InboxCard + SlidesModal                │
                    │  [+ Добавить слайды] → загрузка         │
                    └─────────────────┬───────────────────────┘
                                      │ slides[]
                                      ▼
                    ┌─────────────────────────────────────────┐
                    │         ПОШАГОВАЯ ОБРАБОТКА             │
                    │                                         │
                    │  Parse → Transcribe → Clean             │
                    │                          ↓              │
                    │                   ┌──────┴──────┐       │
                    │                   │ has slides? │       │
                    │                   └──────┬──────┘       │
                    │              yes ↙       ↘ no           │
                    │         ┌────────┐    ┌────────┐        │
                    │         │ SLIDES │    │        │        │
                    │         └───┬────┘    │        │        │
                    │             ↓         ↓        │        │
                    │         Longread (cleaned + slides_text)│
                    │                          ↓              │
                    │         Summary → Chunk → Save          │
                    └─────────────────────────────────────────┘
```

### 2.3 Условное появление шага

```typescript
// StepByStep.tsx
const steps = useMemo(() => {
  const base = ['parse', 'transcribe', 'clean', 'longread', 'summarize', 'chunk', 'save'];
  
  if (initialSlides && initialSlides.length > 0) {
    // Вставляем slides между clean и longread
    const idx = base.indexOf('longread');
    return [...base.slice(0, idx), 'slides', ...base.slice(idx)];
  }
  
  return base;
}, [initialSlides]);
```

---

## 3. UI: Главный экран

### 3.1 InboxCard с поддержкой слайдов

**Без слайдов:**
```
┌─────────────────────────────────────────┐
│ 🎵 2026.01 Форум Табтим (Дмитрук).mp3   │
│    Светлана Дмитрук  •  5:01            │
│ ────────────────────────────────────    │
│ [+ Добавить слайды]                     │
│                                         │
│                        [▶ Пошагово ▼]   │
└─────────────────────────────────────────┘
```

**Со слайдами:**
```
┌─────────────────────────────────────────┐
│ 🎵 2026.01 Форум Табтим (Дмитрук).mp3   │
│    Светлана Дмитрук  •  5:01            │
│ ────────────────────────────────────    │
│ [📎 8 слайдов (15.2 MB) →]              │  ← клик открывает модалку
│                                         │
│                        [▶ Пошагово ▼]   │
└─────────────────────────────────────────┘
```

### 3.2 Компоненты

```typescript
// Новый компонент для отображения слайдов в карточке
interface SlidesAttachmentProps {
  slides: SlideFile[];
  onAdd: () => void;      // Открыть модалку
  onRemove: () => void;   // Удалить все слайды
}

// Тип файла слайда
interface SlideFile {
  id: string;
  name: string;
  size: number;           // bytes
  type: 'image' | 'pdf';
  file?: File;            // Для загрузки
  preview?: string;       // data URL для превью
}
```

---

## 4. UI: Модальное окно слайдов

### 4.1 Макет

```
┌─────────────────────────────────────────────────────────────────┐
│ Слайды презентации                                         ✕    │
│ 2026.01 Форум Табтим. Тестовая тема (Светлана Дмитрук).mp3     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │          📤 Перетащите файлы сюда                         │  │
│  │             или выберите на компьютере                    │  │
│  │                                                           │  │
│  │                  🖼️ JPG, PNG  •  📄 PDF                    │  │
│  │                                                           │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Загружено: 8 файлов (15.2 MB)                  [Удалить все]   │
│                                                                 │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                   │
│  │   1    │ │   2    │ │   3    │ │   4    │                   │
│  │ IMG_01 │ │ IMG_02 │ │ IMG_03 │ │ IMG_04 │                   │
│  │  [🗑️]  │ │  [🗑️]  │ │  [🗑️]  │ │  [🗑️]  │                   │
│  └────────┘ └────────┘ └────────┘ └────────┘                   │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                   │
│  │   5    │ │   6    │ │   7    │ │   8    │                   │
│  │ IMG_05 │ │ IMG_06 │ │ IMG_07 │ │ IMG_08 │                   │
│  │  [🗑️]  │ │  [🗑️]  │ │  [🗑️]  │ │  [🗑️]  │                   │
│  └────────┘ └────────┘ └────────┘ └────────┘                   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ ℹ️ Слайды будут обработаны на отдельном шаге         [Готово]   │
│    перед генерацией лонгрида                                    │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 Функциональность

| Элемент | Действие |
|---------|----------|
| **Drop Zone** | Drag & drop файлов или клик для выбора |
| **Сетка слайдов** | 4 колонки, превью с номером |
| **Hover на слайде** | Показать кнопку удаления |
| **Удалить все** | Очистить список |
| **Готово** | Закрыть модалку, сохранить состояние |

### 4.3 Поддерживаемые форматы

| Формат | Обработка |
|--------|-----------|
| **JPG, PNG, WEBP** | Напрямую в vision API |
| **PDF** | Конвертация страниц в изображения (pdf2image) |

### 4.4 Ограничения

| Параметр | Лимит | Обоснование |
|----------|-------|-------------|
| Макс. файлов | 50 | Ограничение контекста |
| Макс. размер файла | 10 MB | Ограничение API |
| Общий размер | 100 MB | Память браузера |

---

## 5. UI: Шаг обработки слайдов

### 5.1 Карточка шага в timeline

```
┌─────────────────────────────────────────┐
│ ✓  4  Извлечение слайдов                │
│       8 файлов                          │
│       ⚙️ Настройки ▼                    │
└─────────────────────────────────────────┘
```

### 5.2 Панель настроек (развёрнутая)

```
┌─────────────────────────────────────────┐
│ МОДЕЛЬ                                  │
│ ┌─────────────────────────────────────┐ │
│ │ ☁️ Claude Haiku 4.5 (рек.)        ▼ │ │
│ └─────────────────────────────────────┘ │
│ Haiku дешевле. Sonnet/Opus — для        │
│ сложных схем и графиков.                │
│                                         │
│ ПРОМПТЫ                                 │
│ system          user                    │
│ [default ▼]     [default ▼]             │
│                                         │
│ [🔄 Перезапустить]                      │
└─────────────────────────────────────────┘
```

### 5.3 Результат обработки (Result View)

```
┌─────────────────────────────────────────────────────────────────┐
│ Извлечённые данные со слайдов                                   │
│ 8 слайдов  •  1 847 симв.  •  2 таблицы  •  12с                │
│                                              ☁️ claude-haiku-4.5 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ ## Слайд 1: Введение в продукт                                  │
│                                                                 │
│ **Формула 1** — сбалансированное питание для контроля веса.    │
│                                                                 │
│ ### Состав на порцию (26г):                                     │
│ | Нутриент | Количество | % от РСП |                            │
│ |----------|------------|----------|                            │
│ | Белок | 18г | 36% |                                           │
│ | Клетчатка | 3г | 12% |                                        │
│ | Витамин C | 60мг | 100% |                                     │
│                                                                 │
│ ---                                                             │
│                                                                 │
│ ## Слайд 2: Преимущества                                        │
│ ...                                                             │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ Токены: 8 420 вх. / 1 250 вых.                         ~$0.012  │
└─────────────────────────────────────────────────────────────────┘
```

### 5.4 Метрики результата

| Метрика | Источник | Отображение |
|---------|----------|-------------|
| Кол-во слайдов | len(slides) | "8 слайдов" |
| Символы | len(extracted_text) | "1 847 симв." |
| Таблицы | regex count `\|.*\|` | "2 таблицы" |
| Время | processing_time | "12с" |
| Модель | model | badge |
| Токены | tokens.input / output | footer |
| Стоимость | calculated | footer |

---

## 6. Backend: API

### 6.1 Новый endpoint

```
POST /api/step/slides
```

### 6.2 Request

```typescript
interface StepSlidesRequest {
  // Слайды как base64 или multipart
  slides: {
    filename: string;
    content_type: string;  // image/jpeg, image/png, application/pdf
    data: string;          // base64
  }[];
  
  // Настройки
  model?: string;          // claude-haiku-4.5 (default)
  prompt_overrides?: {
    system?: string;
    user?: string;
  };
}
```

### 6.3 Response

```typescript
interface StepSlidesResponse {
  extracted_text: string;     // Markdown с H2 для каждого слайда
  
  // Метрики
  slides_count: number;
  chars_count: number;
  words_count: number;
  tables_count: number;
  
  // LLM метрики
  model: string;
  tokens: {
    input: number;
    output: number;
    total: number;
  };
  cost: number;
  processing_time: number;    // секунды
}
```

### 6.4 Обновление LongreadStage

```typescript
interface StepLongreadRequest {
  cleaned_transcript: string;
  slides_text?: string;        // NEW: извлечённый текст слайдов
  content_type: ContentType;
  model?: string;
  prompt_overrides?: PromptOverrides;
}
```

---

## 7. Backend: SlidesStage

### 7.1 Структура

```
backend/
├── app/
│   ├── services/
│   │   ├── slides_extractor.py      # NEW: извлечение текста
│   │   └── stages/
│   │       └── slides_stage.py      # NEW: stage класс
│   └── api/
│       └── step_routes.py           # +endpoint slides
└── config/
    └── prompts/
        └── slides/
            ├── system.md            # Системный промпт
            └── user.md              # Пользовательский промпт
```

### 7.2 SlidesExtractor

```python
# backend/app/services/slides_extractor.py

from dataclasses import dataclass
from typing import List, Optional
from app.services.ai_clients.base import ChatUsage

@dataclass
class SlideInput:
    filename: str
    content_type: str
    data: bytes  # raw image bytes

@dataclass
class SlidesExtractionResult:
    extracted_text: str
    slides_count: int
    chars_count: int
    words_count: int
    tables_count: int
    model: str
    tokens: dict
    cost: float
    processing_time: float

class SlidesExtractor:
    def __init__(self, ai_client, settings):
        self.ai_client = ai_client
        self.settings = settings
    
    async def extract(
        self,
        slides: List[SlideInput],
        model: Optional[str] = None,
        prompt_overrides: Optional[dict] = None
    ) -> SlidesExtractionResult:
        """
        Извлекает текст со слайдов используя vision API.
        
        Стратегия:
        1. Группировать слайды в батчи (по 5-10)
        2. Отправлять каждый батч в vision API
        3. Собирать результаты с разделителями
        """
        pass
```

### 7.3 SlidesStage

```python
# backend/app/services/stages/slides_stage.py

from app.services.pipeline.base_stage import BaseStage

class SlidesStage(BaseStage):
    name = "slides"
    depends_on = ["parse", "clean"]  # Ждём очистку
    
    def should_skip(self, context: PipelineContext) -> bool:
        """Пропускаем если нет слайдов."""
        return not context.slides or len(context.slides) == 0
    
    async def execute(self, context: PipelineContext) -> StageResult:
        """Извлекаем текст со слайдов."""
        extractor = SlidesExtractor(self.ai_client, self.settings)
        result = await extractor.extract(
            slides=context.slides,
            model=context.model_overrides.get("slides"),
            prompt_overrides=context.prompt_overrides.get("slides")
        )
        return StageResult(
            data={"slides_text": result.extracted_text},
            metrics=result.to_metrics()
        )
```

### 7.4 Интеграция в LongreadGenerator

```python
# backend/app/services/longread_generator.py

class LongreadGenerator:
    async def generate(
        self,
        cleaned_transcript: str,
        slides_text: Optional[str] = None,  # NEW
        content_type: ContentType,
        ...
    ) -> LongreadResult:
        # Формируем контекст
        context_parts = [cleaned_transcript]
        
        if slides_text:
            context_parts.append(
                "\n\n---\n\n"
                "## Дополнительная информация со слайдов презентации\n\n"
                f"{slides_text}"
            )
        
        full_context = "\n".join(context_parts)
        
        # Генерируем лонгрид
        ...
```

---

## 8. Промпты

### 8.1 Расположение

```
config/prompts/slides/
├── system.md              # Роль и правила
└── user.md                # Инструкции по извлечению
```

### 8.2 Системный промпт (system.md)

```markdown
Ты — эксперт по извлечению структурированной информации из слайдов презентаций.

Твоя задача — преобразовать визуальное содержимое слайда в чистый текстовый формат, сохраняя:
- Всю текстовую информацию
- Структуру данных (таблицы, списки)
- Иерархию заголовков
- Числовые данные и статистику
```

### 8.3 Пользовательский промпт (user.md)

> **См. приложение:** `Slides_Extraction_Instructions.md`

---

## 9. Конфигурация моделей

### 9.1 Vision-модели для слайдов

```yaml
# config/models.yaml

slides:
  default: claude-haiku-4.5
  available:
    - id: claude-haiku-4.5
      name: Claude Haiku 4.5
      type: cloud
      vision: true
      pricing:
        input: 0.80
        output: 4.00
      description: "Быстрый и дешёвый. Для текста и простых таблиц."
      
    - id: claude-sonnet-4.5
      name: Claude Sonnet 4.5
      type: cloud
      vision: true
      pricing:
        input: 3.00
        output: 15.00
      description: "Баланс. Для сложных схем и графиков."
      
    - id: claude-opus-4.5
      name: Claude Opus 4.5
      type: cloud
      vision: true
      pricing:
        input: 15.00
        output: 75.00
      description: "Максимальное качество. Для критически важных данных."
```

### 9.2 Рекомендации по выбору

| Контент слайдов | Рекомендуемая модель |
|-----------------|---------------------|
| Текст, списки | Haiku |
| Простые таблицы | Haiku |
| Сложные таблицы | Sonnet |
| Схемы, диаграммы | Sonnet |
| Графики с данными | Sonnet / Opus |
| Рукописные заметки | Opus |

---

## 10. Обработка PDF

### 10.1 Стратегия

```python
# backend/app/utils/pdf_processor.py

import fitz  # PyMuPDF

def pdf_to_images(pdf_bytes: bytes, dpi: int = 150) -> List[bytes]:
    """
    Конвертирует PDF в список изображений.
    
    Args:
        pdf_bytes: PDF файл как bytes
        dpi: Разрешение (150 достаточно для OCR)
    
    Returns:
        Список PNG изображений как bytes
    """
    doc = fitz.open(stream=pdf_bytes, filetype="pdf")
    images = []
    
    for page in doc:
        # Рендерим страницу в изображение
        mat = fitz.Matrix(dpi / 72, dpi / 72)
        pix = page.get_pixmap(matrix=mat)
        images.append(pix.tobytes("png"))
    
    doc.close()
    return images
```

### 10.2 Зависимости

```txt
# requirements.txt
PyMuPDF>=1.23.0
```

---

## 11. Хранение слайдов

### 11.1 В браузере (временно)

```typescript
// Слайды хранятся в state компонента до отправки на обработку
interface InboxFileWithSlides {
  id: string;
  name: string;
  // ...
  slides: SlideFile[];  // File objects + previews
}
```

### 11.2 При отправке на сервер

```typescript
// Конвертация в base64 перед отправкой
async function slidesToBase64(slides: SlideFile[]): Promise<SlideData[]> {
  return Promise.all(slides.map(async (slide) => ({
    filename: slide.name,
    content_type: slide.file.type,
    data: await fileToBase64(slide.file)
  })));
}
```

### 11.3 На сервере (временно)

Слайды не сохраняются на диск — обрабатываются в памяти и отбрасываются после извлечения текста.

---

## 12. Тестирование

### 12.1 Тестовые сценарии

| Сценарий | Ожидаемый результат |
|----------|---------------------|
| Без слайдов | Шаг "Слайды" не появляется |
| 1 слайд JPG | Извлечение успешно |
| 10 слайдов JPG | Батчевая обработка |
| PDF 5 страниц | Конвертация + извлечение |
| Смешанный (JPG + PDF) | Корректная обработка всех |
| Слайд без текста (фото) | Описание изображения |
| Сложная таблица | Markdown таблица |

### 12.2 Метрики качества

| Метрика | Как измерять |
|---------|--------------|
| Полнота извлечения | Ручная проверка на 10 слайдах |
| Корректность таблиц | Сравнение колонок/строк |
| Время обработки | < 3с на слайд (Haiku) |
| Стоимость | < $0.01 на слайд (Haiku) |

---

## 13. План реализации

### Фаза 8.1: Backend (v0.50)

- [ ] `SlidesExtractor` — извлечение текста через vision API
- [ ] `SlidesStage` — stage класс с `should_skip()`
- [ ] `pdf_processor.py` — конвертация PDF в изображения
- [ ] `POST /api/step/slides` — endpoint
- [ ] Промпты `slides/system.md`, `slides/user.md`
- [ ] Тесты извлечения

### Фаза 8.2: Frontend — Главный экран (v0.51)

- [ ] `SlidesAttachment` — компонент в InboxCard
- [ ] `SlidesModal` — модальное окно загрузки
- [ ] Drag & drop + file picker
- [ ] Превью слайдов в сетке
- [ ] Передача slides в StepByStep

### Фаза 8.3: Frontend — Пошаговый режим (v0.52)

- [ ] Условный шаг "slides" в pipeline
- [ ] `SlidesResultView` — отображение результата
- [ ] Настройки шага (модель, промпты)
- [ ] Интеграция с LongreadStage (slides_text)

### Фаза 8.4: Авто-режим (v0.53)

- [ ] Автоматическая обработка слайдов в auto pipeline
- [ ] Прогресс слайдов в общем прогрессе

---

## 14. Референсы

### 14.1 Прототипы UI

- `docs/reference/SlidesPrototype.jsx` — интерактивный прототип всех экранов

### 14.2 Промпты

- `docs/template prompts/Slides_Extraction_Instructions.md` — инструкции извлечения

### 14.3 Связанные документы

- `docs/research/pipeline-optimization-for-rag.md` — общий план pipeline
- `docs/proposals/transcriptor-v2-requirements.md` — референс по формату требований

---

## История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2026-01-23 | 1.0 | Первоначальная версия требований |
